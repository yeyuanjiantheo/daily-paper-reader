Title: Dynamic Objective Selection with Safeguards and LLM Oversight for Financial Decision-Making

URL Source: https://arxiv.org/pdf/2606.03704v1

Published Time: Wed, 03 Jun 2026 01:34:50 GMT

Number of Pages: 10

Markdown Content:
Published as a conference paper at ICLR 2026 

# DYNAMIC OBJECTIVE SELECTION WITH SAFEGUARDS AND LLM O VERSIGHT FOR FINANCIAL DECISION -MAKING 

Keigo Sakurai, Takahiro Ogawa, Miki Haseyama 

Hokkaido University 

sakurai@lmd.ist.hokudai.ac.jp ogawa@lmd.ist.hokudai.ac.jp mhaseyama@lmd.ist.hokudai.ac.jp 

Anjyu Anan 

Nomura Asset Management Co., Ltd. Kobe University 

an2anju555@gmail.com 

Kei Nakagawa 

Osaka Metropolitan University 

kei.nak.0315@gmail.com 

# ABSTRACT 

Financial decision-making tasks such as stock recommendation and portfolio allo-cation typically estimate future return and risk and then select trades or allocations for an investor, and the chosen optimization objective often determines realized per-formance. However, because market conditions evolve over time, a fixed objective can be suboptimal across regimes, while regime-switching pipelines that rely on latent regime estimates can be noisy or delayed and frequent switching can increase turnover and operational instability. In this paper, we propose DOSS (Dynamic Ob-jective Selection with Safeguards), a learning-based selector that directly chooses the decision-relevant objective function at each time point from interpretable statis-tical summaries of recent returns, selecting among a small set of candidates (e.g., return-seeking, loss-averse, and risk-adjusted) without introducing intermediate regime variables. DOSS formulates objective selection as a classification problem over objectives and performs sequential updates with a rolling window to make forward-looking selections without temporal leakage, while also outputting a confi-dence score for each proposal. To mitigate misselection and excessive switching in deployment, DOSS applies confidence-aware gating with a fail-safe that overrides low-confidence proposals to a conservative default and enforces explicit controls tied to switching frequency. We further integrate governance by positioning a Large Language Model (LLM) as an oversight component rather than a generator of new objectives: the LLM is restricted to accept a proposed objective or override it to a predefined safe default, with deterministic rule-based constraints triggering overrides when needed. 

# 1 INTRODUCTION 

In financial decision-making tasks such as stock recommendation (Ren & Malik, 2019; Takayanagi et al., 2023; Lee et al., 2024) and portfolio allocation (Pinelis & Ruppert, 2022; Imajo et al., 2021), a model estimates future return and risk and then selects trades or allocations for an investor. In such systems, the choice of optimization objective often determines realized performance (Markowitz, 1952; Michaud, 1989; Lee, 2011). However, market conditions evolve over time, and a fixed objective can be suboptimal across regimes. For example, a return-seeking objective may be suitable in rising markets, whereas a loss-averse or risk-adjusted objective may be preferable in downturns, motivating objective switching over time (Hamilton, 1989; Nawrocki, 1999; Sortino & Van Der Meer, 1991). Despite this need, many AI/ML formulations for finance commit to a single objective (e.g., maximiz-ing return or optimizing a risk criterion) throughout deployment. Regime-switching pipelines aim to change behavior by inferring latent regimes, but regime estimates are not directly observable and 1

> arXiv:2606.03704v1 [cs.AI] 2 Jun 2026

Published as a conference paper at ICLR 2026 can be noisy or delayed, leading to mistimed switches (Hamilton, 1989; Dacco & Satchell, 1999). Moreover, frequent switching can increase turnover and operational instability, making objective switching difficult to deploy without explicit controls (Shen et al., 2014; Nakagawa et al., 2021). A direct formulation is to select the decision-relevant objective function from observable market features at each time point rather than inferring intermediate regime variables. Such a formulation naturally admits rolling or sequential updates under non-stationary market conditions while preserving a strict forward-looking constraint (Bergmeir & Ben ´ıtez, 2012; Gama et al., 2014). Given interpretable statistical summaries of recent returns, a selector outputs the objective to use from a small set of candidates (e.g., return-seeking, loss-averse, and risk-adjusted), treating objective selection as a policy (a meta-decision) that precedes downstream optimization (Freund & Schapire, 1997). Because objective selection is uncertain, the selector should expose a confidence signal and include a fail-safe that overrides low-confidence proposals to a conservative default, reducing misselection and excessive switching while keeping the basis of selection explicit (Chow, 2003; Guo et al., 2017; Hendrickx et al., 2024). In deployment, objective switching also requires oversight mechanisms that enforce risk constraints and provide accountable decision traces (Crespo et al., 2017). To satisfy such deployment requirements, governance can be integrated by positioning a Large Language Model (LLM) as an oversight component rather than a generator of new objectives. The LLM is restricted to accept a proposed objective or override it to a predefined safe default, while rule-based constraints (e.g., low confidence or excessive switching) enforce overrides deterministically (Bai et al., 2022; Gu et al., 2024). Restricting the LLM to accept/override confines prompt sensitivity and uncertainty to a bounded supervisory role aligned with deployment requirements. In this paper, we propose DOSS (D ynamic O bjective Selection with Safeguards), a learning-based selector that chooses among multiple candidate objective functions at each time point from inter-pretable statistical features summarizing the market state. DOSS formulates objective selection as a classification problem over objectives and performs sequential updates using a rolling window to make forward-looking selections without temporal leakage. DOSS also outputs a confidence score and applies a gating mechanism that falls back to a conservative objective under low confidence, miti-gating misselection and excessive switching. Experiments compare DOSS against static objectives, direct LLM-based selection, and a hindsight oracle, showing consistent improvements over the static baseline on a public benchmark. Our contributions are threefold: • We formulate objective-function switching as a policy problem in financial decision-making, where the system selects an objective at each time point under evolving market conditions. • We propose DOSS, a learning-based objective selector operating on interpretable statisti-cal features with rolling-window updates for forward-looking selection without temporal leakage. • We introduce safeguard and governance mechanisms: confidence-aware gating to a con-servative default and optional LLM oversight constrained to accept/override, to control misselection and excessive switching in deployment-oriented settings. 

# 2 PROBLEM SETTING 

We study sequential financial decision-making at discrete decision times t ∈ T = {1, . . . , T }. At each t, the system observes an information set It and computes interpretable features xt = ϕ(It) ∈

Rd. Before future outcomes are realized, the system selects an optimization objective ot ∈ O from a small candidate set and then executes a fixed downstream decision module. 

Candidate objectives (predefined and bounded). Let O = {o(1) , . . . , o (M )} be a fixed, predefined candidate set; each o ∈ O corresponds to a scalar objective Jo(·; St) used by the downstream optimizer. Throughout, objective switching selects among this bounded set only (no latent regimes and no expansion); in experiments we use O = {RETURN , DOWNSIDE RISK , SHARPE LIKE } as objectives already supported by the benchmark. 2Published as a conference paper at ICLR 2026 

Downstream decision module (fixed). At each t, the downstream module outputs an action 

at = OPTIMIZE (ot; St) ∈ A t, (1) and is held fixed across all compared policies (same solver/seed and constraint/cost modeling), so differences isolate the objective-selection policy. 

Realized utility and offline objective sweep. Let U (t, a ; H) denote the realized utility of executing action a at time t and evaluating over horizon H. For any o ∈ O , define 

u(t, o ) ≜ U (t, OPTIMIZE (o; St); H) . (2) Because |O| is small, we can compute u(t, o ) for all o ∈ O offline (objective sweep). 

Hindsight oracle label (for supervised learning only). The best-in-hindsight objective within the candidate set is 

o⋆t = arg max  

> o∈O

u(t, o ). (3) 

Forward-looking constraint and label availability. The policy must be measurable w.r.t. It:

ot = π(xt) cannot use future data. When training online/rolling, the oracle label o⋆i for a past index 

i is available at time t only if its evaluation horizon has fully realized (i.e., i + H ≤ t), preventing temporal leakage. 

Deployment-oriented safeguards. Objective switching can induce instability. We include (i) confidence gating to a conservative default objective odef ∈ O and (ii) optional switching-frequency control. Let oraw  

> t

be the selector proposal and ct ∈ [0 , 1] a confidence score. The gated objective is 

ot =

odef , ct < τ, oraw  

> t

, otherwise , (4) with threshold τ . We also define a trailing-window switch count over executed objectives: SWITCH COUNT (t; k) = 

> t

X

> j=max {2, t −k+1 }

I[oj̸ = oj−1], (5) which can be constrained to limit excessive objective churn in deployment. 

# 3 METHOD 

DOSS (Dynamic Objective Selection with Safeguards) selects, at each decision time t, a decision-relevant objective ot ∈ O from interpretable features xt, without introducing latent regime variables. The method has four components: (1) oracle label construction for past times (offline only), (2) a rolling-window objective selector trained as a classifier, (3) confidence-aware safeguards (gat-ing and optional switching control), and (4) an optional constrained LLM auditor for governance (accept/override only). An overview of the proposed procedure is summarized in Algorithm 1. 3.1 OVERVIEW 

At time t, DOSS outputs a distribution over candidate objectives pθ (o | xt), proposes the most likely objective, and then applies deterministic safeguards before passing the final objective to the downstream optimizer. Rolling-window retraining adapts to evolving market conditions while preserving a strict forward-looking information constraint. 3.2 ROLLING -WINDOW OBJECTIVE SELECTOR 

Training data with matured oracle labels. Let H be the evaluation horizon used in U (·, ·) (cf. Section 2). At decision time t, define the set of past indices whose oracle labels have fully realized: 

Imature  

> t

≜ {i : t − w ≤ i ≤ t − 1 and i + H ≤ t}. (6) We form a rolling training set Dt = {(xi, y i)}i∈I mature  

> t

, where yi = o⋆i is the hindsight-best objective in Eq. (3). This ensures that at time t the selector is trained only on supervision available by t. This rolling-window update is a standard approach for adapting to non-stationary environments (concept drift) while keeping the training data recent (Gama et al., 2014). 3Published as a conference paper at ICLR 2026 

Algorithm 1 DOSS with safeguards (and optional LLM auditor) 

1: Inputs: candidate objectives O, default odef , window w, horizon H, reg. λ, confidence threshold 

τ , switch window k and max switch smax .

2: For t = 1 to T :

3: Observe info set It and features xt = ϕ(It); form state St.

4: Construct matured index set Imature  

> t

= {i : t − w ≤ i ≤ t − 1, i + H ≤ t}.

5: (Train) Fit θt = arg min θ Lt(θ) on {(xi, o ⋆i )}i∈I mature  

> t

.

6: (Predict) Compute pθt (· | xt) and oraw  

> t

= arg max o∈O pθt (o | xt).

7: (Confidence) Let ct = p(1) (xt) − p(2) (xt).

8: (Gating) Set ˜ot = odef if ct < τ , else ˜ot = oraw  

> t

.

9: (Switch control, optional) If S WITCH COUNT (t; k) > s max then set ˜ot = odef .

10: (LLM audit, optional) Query auditor with (xt, ˜ot, c t); if O VERRIDE then set ot = odef else 

ot = ˜ ot.

11: (Execute) Output action at = OPTIMIZE (ot; St).

12: End for Model and objective. We parameterize the selector as a linear softmax classifier: 

pθ (o | x) = exp( w⊤ 

> o

x + bo)

P 

> o′∈O

exp( w⊤ 

> o′

x + bo′ ) , (7) and minimize regularized cross-entropy over the rolling window: 

Lt(θ) = − 1

|I mature  

> t

|

X

> i∈I mature
> t

log pθ (yi | xi) + λ∥θ∥22. (8) The linear form keeps the selector interpretable and stable under small sample sizes. 

Inference. Given xt, the raw proposal is the MAP objective 

oraw  

> t

= arg max  

> o∈O

pθ (o | xt). (9) 3.3 CONFIDENCE -AWARE SAFEGUARDS 

Confidence score. We define confidence as the probability gap between the top-1 and top-2 objectives: 

ct = p(1) (xt) − p(2) (xt), (10) where p(1) (xt) and p(2) (xt) are the largest and second-largest entries of pθ (· | xt).

Gating (fail-safe default). We apply confidence gating to a conservative default objective odef 

via Eq. (4). This explicitly mitigates misselection when the selector is uncertain, and reduces instability by avoiding low-confidence switches into aggressive objectives. This is closely related to decision-making with a reject option, where uncertain predictions are abstained or mapped to a safe fallback (Chow, 2003; Hendrickx et al., 2024). 

Optional switching-frequency control. To further limit operational churn, we optionally enforce a threshold on SWITCH COUNT (t; k) in Eq. (5). If the threshold is exceeded, the objective is overridden to odef . This safeguard is deterministic and auditable. 3.4 OPTIONAL CONSTRAINED LLM AUDITOR (GOVERNANCE )In deployment, DOSS can be augmented with an LLM used strictly as an auditor, not as an objective generator. The auditor observes (xt, ˜ot, c t) after deterministic safeguards and outputs A CCEPT or OVERRIDE plus a short rationale. If O VERRIDE , the final objective is set to odef . This keeps the decision space bounded ( O) and yields an auditable accept/override trace; we do not evaluate the auditor in the candidate-only experiments. 4Published as a conference paper at ICLR 2026 

# 4 EXPERIMENTS 

4.1 EXPERIMENTAL SETTINGS 

4.1.1 BENCHMARK AND EVALUATION PROTOCOL 

We instantiate DOSS on FAR-Trans (Sanz-Cruzado et al., 2024), a public benchmark for financial asset recommendation with rolling evaluation over market time. Following the benchmark protocol, we evaluate over |T | = 61 decision time points. Each time point defines a split where (i) market and transaction information prior to t forms the available information, and (ii) outcomes in the subsequent 

(t, t + ∆ t) interval form the evaluation period, with ∆t = 6 months and time points spaced two weeks apart. 1

In this instantiation, the evaluation horizon in Section 2 corresponds to a six-month interval. We denote by H the number of decision steps spanning this interval (i.e., H is in decision-time units consistent with the index arithmetic in Section 3). Therefore, at time t, oracle labels are available only for past indices whose six-month evaluation windows have fully realized, matching the matured-label constraint in Section 3. We treat each benchmark time point as a decision time t in Section 2. 4.1.2 CANDIDATE OBJECTIVES AND FIXED DOWNSTREAM MODULE 

We evaluate in the candidate-only setting: the downstream decision module OPTIMIZE (·) and its constraints are fixed, and only the objective-selection policy changes. We instantiate the bounded candidate set as O = {RETURN , DOWNSIDE RISK , SHARPE LIKE }, representing return-seeking, downside-risk (loss-averse), and risk-adjusted (Sharpe-like), respectively. At each t, the downstream module receives (ot, St) and outputs a feasible action (e.g., portfolio weights or a ranked list) consistent with implementation constraints (budget/long-only/position limits, and any transaction-cost terms if included). For offline analysis, we compute u(t, o ) for all o ∈ O by running the fixed downstream module under each candidate objective. 4.1.3 OBJECTIVE -SELECTION METRIC AND STABILITY STATISTICS 

Let u(t, o ) be the realized utility over the benchmark evaluation horizon when objective o is used at t

(see Section 2). We measure objective-selection quality via the hit indicator with tolerance ϵ > 0. Let ot = π(xt) be the objective selected by policy π at time t. Then 

hit( t; π) = I



u(t, o t) ≥ max  

> o∈O

u(t, o ) − ϵ



. (11) and report Hit Percentage Accuracy (HPA): HPA (π) = 1

|T | 

X

> t∈T

hit( t; π). (12) We use ϵ = 0 .02 in all experiments. Because deployment concerns include operational instability, we also report: (i) switch rate =

> 1
> |T |− 1

P 

> t> 1

I[ot̸ = ot−1] and (ii) gating rate = 1

> |T |

P 

> t

I[ct < τ ] for confidence gating (applicable to gated policies such as DOSS–G ATE DOWN ). 4.1.4 COMPARED POLICIES 

We compare: • DOSS–G ATE DOWN : DOSS with confidence gating to a conservative default odef =

DOWNSIDE RISK and threshold τ . Unless stated otherwise, we report a representative operating point τ = 0 .2 (and include a threshold sweep).    

> 1We use FAR-Trans’ standard rolling construction: 61 dataset variants, ∆t= 6 months, two-week spacing.

5Published as a conference paper at ICLR 2026 Table 1: Candidate-only objective-selection results on FAR-Trans ( |T | = 61 , ϵ = 0 .02 ). HPA measures how often the selected objective is within ϵ of the best candidate objective at each time point. Switch rate is the fraction of adjacent time points where the selected objective changes. Figure 1 plots the cumulative HPA counterpart of Table 1. Policy HPA Switch rate DOSS–G ATE DOWN (τ = 0 .2) 0.5626 0.4590 

Static (D OWNSIDE RISK ) 0.5235 0.0000 

Static (R ETURN ) 0.4765 0.0000 

Static (S HARPE LIKE ) 0.4850 0.0000 

LLM-Direct (GPT-4.1-MINI ) 0.5173 0.5902 

LLM-Direct (M OCK ) 0.5433 0.4754 

Oracle 0.6150 0.8197 • Static (o): fixed objective o ∈ O for all t.• LLM-Direct (GPT-4.1-mini) : an LLM directly predicts an objective in O from the same interpretable features xt. We use gpt-4.1-mini via the OpenAI API (accessed on 

2026-02-05 ), with temperature 0.0 (and top p = 1 .0) for determinism. The prompt pro-vides xt and instructs the model to output exactly one label in {return , downside risk ,

sharpe like }.• LLM-Direct (Mock) : a deterministic heuristic mapping from xt to an objective in O, used as a non-stochastic proxy baseline. • Oracle : the hindsight upper bound o⋆t = arg max o∈O u(t, o ) (offline only). Our main results are for objective selection; the optional constrained LLM auditor in Section 3 is a deployment extension and is not required for the candidate-only comparisons. 4.1.5 IMPLEMENTATION DETAILS 

We use a rolling window size w = 30 time points. At each decision time t, DOSS retrains the linear softmax selector on matured labels (only indices i whose evaluation horizon has realized), as specified in Section 3. We set λ = 0 .01 for ℓ2 regularization. For confidence gating, we sweep 

τ ∈ { 0.0, 0.1, 0.2, 0.3, 0.4} and report both HPA and gating rate. 

LLM prompting. For LLM-Direct (GPT-4.1-mini), we format xt as a compact JSON dictionary of feature names/values and request exactly one label in the bounded set {return , downside risk ,

sharpe like } with temperature 0.0. We map these labels to objectives in O as return 7→ 

RETURN , downside risk 7→ DOWNSIDE RISK , and sharpe like 7→ SHARPE LIKE .4.2 RESULTS 

4.2.1 MAIN RESULTS (CANDIDATE -ONLY )Table 1 reports both objective-selection accuracy (HPA) and operational stability measured by switch rate. Our main configuration, DOSS–G ATE DOWN (τ = 0 .2), achieves HPA = 0 .5626 , improving over the strongest static objective Static(D OWNSIDE RISK ) ( 0.5235 ) by +0 .0391 absolute (about 

+7 .5% relative). Because the candidate set O and the downstream decision module are fixed, this difference is attributable to the objective-selection policy. A key deployment consideration is whether improved accuracy comes at the cost of excessive switching. The hindsight oracle attains the highest HPA ( 0.6150 ) but exhibits a high switch rate (0.8197 ), reflecting aggressive hindsight switching that is typically infeasible or undesirable in practice. In contrast, DOSS–G ATE DOWN achieves a substantially lower switch rate ( 0.4590 ) while closing a meaningful fraction of the oracle gap. Compared with the direct LLM selector, DOSS achieves higher HPA ( 0.5626 vs. 0.5173 ) with lower switching ( 0.4590 vs. 0.5902 ). Figure 1 provides a complementary view by plotting the cumulative HPA over time. 6Published as a conference paper at ICLR 2026 

Figure 1: Cumulative objective-selection accuracy over time (candidate-only). We report the cumu-lative HPA up to each time index for DOSS–G ATE DOWN (τ = 0 .2), the strongest static baseline Static(D OWNSIDE RISK ), and the hindsight oracle (upper bound). Table 2: Sensitivity of DOSS to the confidence threshold τ under confidence gating (G ATE DOWN ). Gating rate is the fraction of time points where the selector falls back to the conservative default 

odef = DOWNSIDE RISK . All entries use the same downstream module and candidate set; only the objective-selection policy changes. 

τ HPA Gating rate 0.0 0.5191 0.00 0.1 0.5491 0.24 0.2 0.5626 0.41 0.3 0.5439 0.56 0.4 0.5155 0.74 4.2.2 SENSITIVITY TO CONFIDENCE THRESHOLD 

Table 2 characterizes the accuracy–conservatism trade-off governed by the confidence threshold τ .Disabling gating ( τ = 0 ) reduces HPA to 0.5191 , indicating that acting on all raw proposals can be harmful when the selector is uncertain. A moderate threshold ( τ = 0 .2) yields the best HPA ( 0.5626 )while gating at a rate of 0.41 , supporting the interpretation of gating as a fail-safe that selectively rejects low-confidence switches. When τ becomes too large, gating dominates the decision rule: the selector falls back to the conserva-tive default too often (gating rate 0.74 at τ = 0 .4), effectively collapsing toward a near-static strategy and losing adaptivity (HPA 0.5155 ). Overall, the sweep exhibits a natural “U-shaped” behavior: insufficient gating admits noisy switches, whereas overly conservative gating removes the benefit of objective switching. 4.2.3 QUALITATIVE BEHAVIOR OVER TIME 

Figure 2 visualizes both the raw selector proposals oraw  

> t

and the final objectives ot after safeguards for DOSS–G ATE DOWN (τ = 0 .2). The two series frequently coincide, indicating that when the selector is confident, its proposed objective is typically executed as-is. Override markers highlight time points where safeguards replace the raw proposal with the conservative default odef , preventing potentially unstable transitions under uncertainty. Together with the moderate switch rate in Table 1, this suggests that DOSS does not “chase” short-lived fluctuations but instead switches objectives selectively. Figure 1 shows that DOSS maintains an advantage over the strongest static baseline across the evaluation window, rather than relying on a few isolated time points. 4.2.4 STATISTICAL RELIABILITY : BOOTSTRAP OVER TIME POINTS 

We assess whether the improvement over the strongest static baseline is robust across time points. Let ∆t = hit( t; πDOSS ) − hit( t; πStaticDown ) and ¯∆ = 1

> |T |

P 

> t

∆t. We perform a nonparametric 7Published as a conference paper at ICLR 2026 

Figure 2: Objective-selection timeline for DOSS–G ATE DOWN (τ = 0 .2). We plot the raw proposal 

oraw  

> t

and the final objective ot after deterministic safeguards. Markers indicating “override” corre-spond to time points where safeguards replace the raw proposal with the conservative default odef 

(e.g., triggered by confidence gating when ct < τ ). Table 3: Bootstrap over time points for DOSS–G ATE DOWN (τ = 0 .2) vs. the strongest static baseline (candidate-only). We report mean ∆HPA, the 95% bootstrap confidence interval, and the probability that the mean improvement is positive. P ( ¯∆ > 0) is estimated from the bootstrap replicates of the mean difference. Comparison Mean ∆HPA 95% CI P ( ¯∆ > 0)       

> DOSS–G ATE DOWN (τ= 0 .2) vs Static (D OWNSIDE RISK )0.0391 [0.0118, 0.0706] 0.9986

bootstrap by sampling time points with replacement and recomputing ¯∆. Table 3 reports a mean improvement of ¯∆ = 0 .0391 in HPA, a 95% bootstrap confidence interval of [0 .0118 , 0.0706] , and 

P ( ¯∆ > 0) = 0 .9986 .We note that time points are not strictly i.i.d. due to temporal dependence; we interpret the bootstrap as a pragmatic robustness check on whether the improvement is driven by a small number of outliers. 4.2.5 DISCUSSION : BOUNDED ADAPTIVITY UNDER OPERATIONAL CONSTRAINTS 

The results illustrate a practical tension in financial deployment: adaptivity can improve outcomes, but frequent behavioral changes can be costly. Static policies incur zero switching but cannot react to evolving market conditions, whereas the oracle achieves the highest HPA with a very high switch rate. DOSS provides a controlled middle ground: it captures a meaningful portion of the oracle headroom while keeping switching substantially lower, and the confidence gating sweep shows that a moderate level of conservative fallback improves accuracy rather than merely reducing churn. A further benefit of objective switching in a bounded candidate set is traceability. Each decision time produces an auditable record consisting of (i) interpretable feature summaries xt, (ii) the selected objective label ot ∈ O , and (iii) the confidence signal used to trigger fallbacks. This structure aligns with governance requirements and motivates the optional LLM auditor in Section 3 as a supervised accept/override layer that does not expand the decision space beyond O.

# 5 CONCLUSION 

We studied objective-function switching as a policy problem in financial decision-making: at each decision time, select a decision-relevant objective from a small predefined candidate set using only observable, interpretable summaries of recent returns. We proposed DOSS, a rolling-window objective selector with confidence-aware safeguards that (i) expose uncertainty via a confidence score and (ii) provide deployment-oriented fail-safes through deterministic gating (and optional switching control). In a candidate-only evaluation on FAR-Trans, DOSS improves objective-selection accuracy over static objectives and a direct LLM selector, while explicitly controlling operational instability through conservative fallback. A constrained LLM auditor can be layered for governance without expanding the action space beyond O.8Published as a conference paper at ICLR 2026 

# REFERENCES 

Yuntao Bai, Saurav Kadavath, Sandipan Kundu, Amanda Askell, Jackson Kernion, Andy Jones, Anna Chen, Anna Goldie, Azalia Mirhoseini, Cameron McKinnon, et al. Constitutional ai: Harmlessness from ai feedback. arXiv preprint arXiv:2212.08073 , 2022. Christoph Bergmeir and Jos ´e M Ben ´ıtez. On the use of cross-validation for time series predictor evaluation. Information Sciences , 191:192–213, 2012. C Chow. On optimum recognition error and reject tradeoff. IEEE Transactions on Information Theory , 16(1):41–46, 2003. Ignacio Crespo, Pankaj Kumar, and Peter Noteboom. The evolution of model risk management. Technical report, McKinsey Global Institute, 2017. Robert Dacco and Steve Satchell. Why do regime-switching models forecast so badly? Journal of forecasting , 18(1):1–16, 1999. Yoav Freund and Robert E Schapire. A decision-theoretic generalization of on-line learning and an application to boosting. Journal of Computer and System Sciences , 55(1):119–139, 1997. Jo ˜ao Gama, Indr ˙e ˇZliobait ˙e, Albert Bifet, Mykola Pechenizkiy, and Abdelhamid Bouchachia. A survey on concept drift adaptation. ACM Computing Surveys , 46(4):1–37, 2014. Jiawei Gu, Xuhui Jiang, Zhichao Shi, Hexiang Tan, Xuehao Zhai, Chengjin Xu, Wei Li, Yinghan Shen, Shengjie Ma, Honghao Liu, et al. A survey on llm-as-a-judge. The Innovation , 2024. Chuan Guo, Geoff Pleiss, Yu Sun, and Kilian Q Weinberger. On calibration of modern neural networks. In Proceedings of the International Conference on Machine Learning , pp. 1321–1330, 2017. James D Hamilton. A new approach to the economic analysis of nonstationary time series and the business cycle. Econometrica: Journal of the Econometric Society , pp. 357–384, 1989. Kilian Hendrickx, Lorenzo Perini, Dries Van der Plas, Wannes Meert, and Jesse Davis. Machine learning with a reject option: A survey. Machine Learning , 113(5):3073–3110, 2024. Kentaro Imajo, Kentaro Minami, Katsuya Ito, and Kei Nakagawa. Deep portfolio optimization via distributional prediction of residual factors. In Proceedings of the AAAI conference on artificial intelligence , volume 35, pp. 213–222, 2021. Wai Lee. Risk-based asset allocation: A new answer to an old question? Journal of Portfolio Management , 37(4):11–28, 2011. Youngbin Lee, Yejin Kim, Javier Sanz-Cruzado, Richard McCreadie, and Yongjae Lee. Stock recommendations for individual investors: A temporal graph network approach with mean-variance efficient sampling. In Proceedings of the ACM International Conference on AI in Finance , pp. 795–803, 2024. Harry Markowitz. Portfolio selection. The Journal of Finance , 7(1):77–91, 1952. Richard O Michaud. The markowitz optimization enigma: Is ‘optimized’optimal? Financial Analysts Journal , 45(1):31–42, 1989. Kei Nakagawa, Shuhei Noma, and Masaya Abe. Rm-cvar: regularized multiple β-cvar portfolio. In 

Proceedings of the Twenty-Ninth International Conference on International Joint Conferences on Artificial Intelligence , pp. 4562–4568, 2021. David N Nawrocki. A brief history of downside risk measures. Journal of Investing , 8:9–25, 1999. Michael Pinelis and David Ruppert. Machine learning portfolio allocation. The Journal of Finance and Data Science , 8:35–54, 2022. Ke Ren and Avinash Malik. Investment recommendation system for low-liquidity online peer to peer lending (p2pl) marketplaces. In Proceedings of the ACM International Conference on Web Search and Data Mining , pp. 510–518, 2019. 9Published as a conference paper at ICLR 2026 Javier Sanz-Cruzado, Nikolaos Droukas, and Richard McCreadie. Far-trans: An investment dataset for financial asset recommendation. In Proceedings of the IJCAI Workshop on Recommender Systems in Finance , 2024. Weiwei Shen, Jun Wang, and Shiqian Ma. Doubly regularized portfolio with risk minimization. In 

proceedings of the AAAI Conference on Artificial Intelligence , volume 28, 2014. Frank A Sortino and Robert Van Der Meer. Downside risk. Journal of Portfolio Management , 17(4): 27, 1991. Takehiro Takayanagi, Chung-Chi Chen, and Kiyoshi Izumi. Personalized dynamic recommender system for investors. In Proceedings of the International ACM SIGIR Conference on Research and Development in Information Retrieval , pp. 2246–2250, 2023. 10