Title: Uncertainty Quantification in Data-Driven Inverse Optimization via Bayesian Inference

URL Source: https://arxiv.org/pdf/2605.25288v1

Published Time: Tue, 26 May 2026 01:35:54 GMT

Number of Pages: 27

Markdown Content:
# Uncertainty Quantification in Data-Driven Inverse Optimization via Bayesian Inference 

Timothy C. Y. Chan 

> Department of Mechanical and Industrial Engineering, University of Toronto

Nathan Sandholtz 

> Department of Statistics, Brigham Young University

Nasrin Yousefi 

> Smith School of Business, Queen’s University

Abstract. Inverse optimization (IO) is used to estimate unknown parameters of an optimization model from observed decisions. In the data-driven context, the estimated parameters are inherently uncertain, yet quantifying this uncertainty has received limited attention in the literature, where existing methods return a point estimate. In this paper, we propose a hierarchical Bayesian framework for parameter uncertainty quantification in data-driven inverse optimization. Considering two data-generating processes, we develop two Markov chain Monte Carlo algorithms to estimate the posterior distribution of the unknown parameter vector, which is used to construct credible regions. We establish posterior consistency under standard identifiability conditions. Numerical experiments demonstrate near-nominal empirical coverage of the credible regions and show that the regions shrink as the number of observed decisions increases. 

Key words: inverse optimization, Bayesian inference, credible region, Markov chain Monte Carlo, hierarchical Bayesian model 

1. Introduction 

Inverse optimization (IO) refers to the problem of estimating parameters of an optimization model using decision data. These estimates can provide insight into the decision-making process that generated the data (Bertsimas et al. 2015, Aswani et al. 2018, Ajayi et al. 2022, Besbes et al. 2025, Ren et al. 2025) or be used in a downstream “forward” optimization model that prescribes decisions (Keshavarz et al. 2011, Chan et al. 2014, R¨ onnqvist et al. 2017, Zattoni Scroccaro et al. 2025b). Existing data-driven inverse optimization approaches generally produce a “point estimate”. However, quantifying the uncertainty in these estimates is critical. For example, this quantification can help decision makers calibrate confidence in their inferences or enable them to develop uncertainty sets for downstream robust optimization (Lin et al. 2024). In this paper, we develop a novel approach to quantify uncertainty in parameters estimated via data-driven inverse optimization, focusing on convex forward problems. We propose a fully gen-erative probabilistic model of the observed decision data, explicitly parameterized by the assumed forward problem. We then leverage this probabilistic model to infer the parameters via Bayesian inference, yielding both a point estimate and a credible region around that estimate.  

> 1
> arXiv:2605.25288v1 [math.OC] 24 May 2026 Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO
> 2

Key to our framework is the explicit consideration of a data model. Accordingly, IO-based parameter estimation should incorporate a data model that is consistent with the decision maker’s belief about how the data was generated. This paper considers two natural data-generating models. The first assumes that decision data is generated by random perturbations of an optimal solution to an optimization problem. In other words, data noise originates in the space of decisions , for example from measurement error. The second assumes the objective function coefficients have been randomly perturbed and the decision maker observes the decisions associated with solving the optimization problem using the perturbed parameters. In this case, noise originates in the space of the objective function , for example arising from imperfect knowledge of the true objective function. By modeling the data-generating process probabilistically, one can view optimization through the lens of statistical inference. In both processes described above, observed decisions are linked to underlying parameters through intermediate, unobserved quantities (either the optimal solution before it is perturbed or the perturbed parameter vector before the forward model is solved). This induces a hierarchical structure in which optimization is an intermediate mapping between parameters and observations. Bayesian methods accommodate optimization-generated intermediate variables by conditioning directly on them rather than requiring closed-form marginalization. Our specific contributions are as follows. 1. We propose the first methodology for quantifying uncertainty in inverse optimization using a statistically rigorous approach based on Bayesian inference. Our approach inherits classical convergence properties of Bayesian inference. In particular, we show that under standard assumptions the posterior distribution concentrates around the true parameter vector. While our primary focus is uncertainty quantification, the proposed method also yields a new approach to solving data-driven inverse optimization problems. 2. We develop two Markov chain Monte Carlo (MCMC)-based algorithms to estimate the unknown parameter vector and construct its credible region. The algorithms are tailored to two data-generating processes, requiring the solution of distinct optimization problems. The first solves forward optimization problems, while the second solves inverse optimization problems. 3. Through extensive numerical experiments on linear and quadratic forward problems, we validate that our algorithms successfully converge to the true parameter vectors. We show that the credible regions shrink as the number of data points increases and that they are well-calibrated to the specified confidence level. Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 3

2. Literature Review 

The inverse optimization literature can be divided into classical and data-driven models (Chan et al. 2025). Classical models determine parameters of an optimization problem assuming that observed decisions are exactly optimal (i.e., noise free). The models may be linear (Ahuja and Orlin 2001), nonlinear (Chow et al. 2014), conic (Iyengar and Kang 2005, Zhang and Xu 2010), or discrete (Schaefer 2009, Wang 2009, Bulut and Ralphs 2021, Bodur et al. 2022). Data-driven models assume observed decisions are noisy and thus may not be optimal for any set of parameters of the chosen model (Keshavarz et al. 2011, Bertsimas et al. 2015, Esfahani et al. 2018, Aswani et al. 2018, Chan et al. 2019, Babier et al. 2021, Chan et al. 2022a,b, Gupta and Zhang 2022, Zattoni Scroccaro et al. 2025a). Our work falls within the data-driven paradigm. Statistical concepts have received some attention in the inverse optimization literature. Aswani et al. (2018) develop an inverse optimization approach that produces statistically consistent estimates for parameters of a convex forward problem. Rather than constructing a single consistent estimator, by assuming a fully probabilistic generative model for the data, we obtain a full posterior distribution and prove that it concentrates around the true parameter vector under analogous identifiability conditions. Shahmoradi and Lee (2022) show that existing inverse LP methods can lead to unstable estimates with noisy data. They develop an inverse model using a quantile statistic of optimality errors to improve stability of the estimates. The formulations from both papers are NP-hard. Also, in all papers described so far, the models only return a point estimate. Birge et al. (2022) study inverse LP where the cost vector is assumed to be drawn from a parametric distribution and the goal is to estimate parameters of this distribution using the observed decisions. Although this approach considers the distributional nature of the parameters, it does not aim to quantify uncertainty in their estimation, which is the focus of our paper. The conformal inverse optimization approach of Chan et al. (2024) generates an uncertainty set over the parameters and uses it in a downstream robust optimization problem. However, their approach does not explicitly model the data-generating process. In contrast, our approach assumes a fully generative probabilistic model for the decision data, and then leverages Bayesian inference to generate a posterior distribution over the parameter vector, which can be used to construct credible regions at arbitrary credibility levels. A few studies have applied Bayesian methods in inverse optimization. Sandholtz et al. (2023) develop a probabilistic framework for inverse Bayesian optimization to estimate human acquisi-tion functions based on observed decisions in exploration–exploitation tasks. Their method defines a likelihood over behavior driven by a Bayesian optimization subroutine, enabling uncertainty Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 4

quantification via credible intervals. However, it relies on discretized acquisition families, lacks theoretical consistency guarantees, and is tailored to a specific behavioral setup. Lu et al. (2025) propose a Bayesian optimization framework for estimating unknown parameters in inverse optimiza-tion, treating the inverse problem as a black-box loss minimization task. Uncertainty quantification is addressed via profile likelihood, a classical frequentist technique for constructing confidence intervals. However, their paper does not formulate a fully generative probabilistic model for the inverse problem nor does it establish asymptotic guarantees such as posterior consistency. 

3. Statistical Motivation and Background 

To motivate our approach, it is instructive to contrast loss-based and likelihood-based regression and to review hierarchical Bayesian models. 

3.1. Loss-based vs. Likelihood-based Regression 

Consider a dataset D = {( u1, 𝑦 1), (u2, 𝑦 2), . . . , (u𝑁 , 𝑦 𝑁 )} . Each 𝑦 𝑖 ∈ R is a realization of a random variable 𝑌 𝑖 generated from a mean function 𝜇 (u𝑖 ; 𝜽 ) = E(𝑌 𝑖 | u𝑖 ) plus random error, where u𝑖 is a vector of covariates and 𝜽 ∈ Rℎ is a parameter vector. The regression problem is to estimate 𝜽 given 

D. This is typically done via: 1) loss-based minimization, or 2) likelihood-based maximization. In loss-based regression, a loss function ℓ (e.g., squared error loss) penalizes the difference between observed 𝑦 𝑖 values and the estimated mean function at the corresponding covariate vector 

u𝑖 . The parameter vector 𝜽 is then computed by minimizing the total loss over the dataset: ˆ𝜽 LOSS ∈ arg min 

> 𝜽
> 𝑁

∑︁ 

> 𝑖 =1

ℓ  𝑦 𝑖 − 𝜇 (u𝑖 ; 𝜽 ) .

Notably, this approach provides no direct uncertainty quantification of the resulting point estimate. In likelihood-based regression, a probabilistic model 𝑓 for the target data is specified (e.g., 

𝑌 𝑖 ∼ N ( 𝜇 (u𝑖 ; 𝜽 ), 𝜎 2)). The likelihood function is defined as the joint density of the observed data: 

𝐿 (𝜽 ; D) = 𝑓 



{𝑦 𝑖 }𝑁 𝑖 =1 {u𝑖 }𝑁 𝑖 =1, 𝜽 



.

Inference may proceed under a Bayesian or frequentist paradigm. For the Bayesian approach, a prior distribution over 𝜽 , 𝜋 (𝜽 ), is specified and Bayes’ rule is used to obtain the posterior distribution 

𝜋 (𝜽 | D) ∝ 𝐿 (𝜽 ; D) 𝜋 (𝜽 ), (1) Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 5

representing the updated belief about 𝜽 conditional on the observed data D. Point estimates for 𝜽 

are usually based on posterior summaries such as the posterior mean, median, or mode. Uncertainty can be summarized by a (1 − 𝛼 )-level credible region, C1−𝛼 , which satisfies 

∫ 

> C1−𝛼

𝜋 (𝜽 | D) 𝑑 𝜽 ≥ 1 − 𝛼. 

Relating these paradigms to data-driven inverse optimization, the IO approaches in the literature are almost exclusively loss-based: model parameters 𝜽 are chosen to minimize some loss between the observed decisions and optimal decisions without considering the distribution of the decision data. The approach we propose is likelihood-based: we treat observed decisions as realizations from the composition of a probabilistic model and an optimization model, defined in Section 4. 

3.2. Hierarchical Bayesian Models 

Hierarchical Bayesian models (HBMs) represent complex data-generating processes by decom-posing them into conditional layers. As a generic example, consider a model that generates an observation y from the parameter vector 𝜽 via the intermediate (latent) variable z:

𝜽 ∼ 𝜋 (·) , z ∼ 𝑝 (· | 𝜽 ), y ∼ 𝑓 (· | z). (2) The intermediate z need not be directly observed and may encode structural relationships in the data-generating process (Gelman and Hill 2007). Under this specification, y is conditionally independent of 𝜽 given z (i.e., 𝑓 (y | z, 𝜽 ) = 𝑓 (y | z)), so that the likelihood depends on 𝜽 only through the intermediate layer. This conditional independence structure is a common and convenient modeling choice in hierarchical Bayesian models, as it simplifies the specification of the joint distribution and posterior computation via MCMC (Gelman et al. 2013, Gelman and Hill 2007). Hierarchical representations are useful when the mapping from parameters to observations is indirect. Instead of specifying a marginal likelihood ℎ(y | 𝜽 ) in closed form, one can model it through its conditional components. Bayesian inference proceeds by operating on these components, avoiding the need to compute the (possibly intractable) marginal ℎ(y | 𝜽 ) := ∫ 𝑓 (y | z) 𝑝 (z | 𝜽 ) 𝑑 z.

Mapping this back to optimization, observed decisions ( y) are linked to model parameters ( 𝜽 )through the solution of an optimization problem, naturally inducing a hierarchical structure. 

4. Inverse Optimization: Loss vs. Likelihood 

We consider a data-driven IO model where observed decisions are associated with covariates that affect the feasible region and/or objective function of the forward problem. The goal is to estimate a parameter vector in the objective function given covariate-decision pairs. Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 6

4.1. Loss-based Inverse Optimization 

This section reviews the “standard” approach to IO. Let the forward problem be 

FO (u, 𝜽 ) : minimize  

> x

𝑔 (x; u, 𝜽 )

subject to x ∈ X( u),

(3) where x ∈ R𝑛 is the decision vector, u ∈ U is a covariate vector, and 𝜽 ∈ Θ ⊆ Rℎ is a nonzero, unknown parameter vector to be estimated via inverse optimization. The feasible region X( u) ⊆ R𝑛 

is assumed to be compact, convex and full-dimensional. We assume 𝑔 is convex in x and linear in 

𝜽 . Since 𝑔 is linear in 𝜽 , the optimal solution set is invariant under positive rescaling of 𝜽 . Thus, we assume that Θ is constrained to the unit hypersphere Sℎ−1 := {𝜽 ∈ Rℎ | ∥ 𝜽 ∥2 = 1}.Assume the forward problem is solved 𝑁 times, each time with a different covariate vector u𝑖 , 𝑖 =

1, . . . , 𝑁 , and the decision maker observes decision data y𝑖 , 𝑖 = 1, . . . , 𝑁 . Specific data-generating processes are described in Section 4.2. Thus, the dataset available to the inverse optimizer is 

D = {( u1, y1), (u2, y2), . . . , (u𝑁 , y𝑁 )} . Given D, the data-driven inverse optimization problem is 

IO (D) : minimize 

> 𝜽
> 𝑁

∑︁ 

> 𝑖 =1

ℓ  y𝑖 , X★(u𝑖 , 𝜽 )

subject to 𝜽 ∈ Θ,

(4) where 

X★(u, 𝜽 ) := arg min 

> x∈X( u)

𝑔 (x; u, 𝜽 ) (5) is the set of optimal solutions to FO (u, 𝜽 ) under u and 𝜽 , and ℓ quantifies the loss between an observed decision y𝑖 and the optimal solution set X★(u𝑖 , 𝜽 ).1 The two most common choices for the loss function are the Euclidean distance between y𝑖 and the optimal solution set ( decision loss ), 

ℓ𝑑 

 y𝑖 , X★(u𝑖 , 𝜽 ) := min     

> z𝑖 ∈X ★(u𝑖 ,𝜽 )

∥y𝑖 − z𝑖 ∥2

and the difference between the objective value of y𝑖 and the optimal value ( suboptimality loss ), 

ℓ𝑜 

 y𝑖 , X★(u𝑖 , 𝜽 ) := 𝑔 (y𝑖 ; u𝑖 , 𝜽 ) − 𝑔 (z𝑖 ; u𝑖 , 𝜽 )

for any z𝑖 ∈ X ★(u𝑖 , 𝜽 ).Solving (4) yields a point estimate ˆ𝜽 LOSS ∈ arg min 

> 𝜽 ∈𝚯
> 𝑁

∑︁ 

> 𝑖 =1

ℓ  y𝑖 , X★(u𝑖 , 𝜽 ) . (6) This loss-based approach does not consider uncertainty around ˆ 𝜽 LOSS . Also, different loss functions will yield different estimates, emphasizing the need to provide uncertainty quantification. Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

7

4.2. Likelihood-based Inverse Optimization 

Here, we introduce a likelihood-based approach to IO. We statistically model the data-generating process that produces the observed decisions. This induces a probability distribution over the obser-vations that we use to construct a set of likely values for 𝜽 . Next, we describe two data-generating processes (also referred to as error processes) that align with decision loss and suboptimality loss. 

4.2.1. Decision-Space Error. This error process assumes perturbations are applied directly to optimal solutions of the forward problem, so that the inverse optimizer observes noisy realizations of these solutions. Let x★(u𝑖 , 𝜽 ∗) be an optimal solution to FO (u, 𝜽 ∗). Conditional on x★(u𝑖 , 𝜽 ∗),and for a given probability distribution 𝑓 on R𝑛 , the decision maker observes 

y𝑖 ∼ 𝑓 (· | x★(u𝑖 , 𝜽 ∗)) . (7) Figure 1a shows a graphical model depicting this data-generating process.               

> 𝜽 ∗
> x1
> x𝑁
> y1
> y𝑁
> Objective Space Decision Space
> ......
> FO (u1,𝜽 ∗)
> FO (u𝑁 ,𝜽 ∗)
> 𝑓
> 𝑓
> (a) Decision-space error
> 𝜽 ∗
> ˜𝜽 1
> ˜𝜽 𝑁
> y1
> y𝑁
> Objective Space Decision Space
> ......
> 𝑝
> 𝑝
> FO (u1,˜𝜽 1)
> FO (u𝑁 ,˜𝜽 𝑁 )
> (b) Objective-space error
> Figure 1 Graphical models illustrating the data-generating process under decision-space (a) and objective-space error (b).

EXAMPLE 1. Figure 2 illustrates two-dimensional minimization problems with a linear objective, under both linear constraints and quadratic constraints. Three instances are depicted in each panel, each corresponding to a different u𝑖 . Instance 𝑖 has a unique optimal solution x𝑖 := x★(u𝑖 , 𝜽 ∗). The decision maker observes y𝑖 , where y𝑖 equals x𝑖 plus Gaussian noise. Note that the observations can be feasible or infeasible solutions. 

4.2.2. Objective-Space Error. This error process assumes that the parameter vector 𝜽 ∗ is perturbed before the forward problem is solved. The resulting optimal solution is what the inverse optimizer observes. Let 𝑝 be a probability distribution over the unit hypersphere Sℎ−1. Then, 

y𝑖 = x★(u𝑖 , ˜𝜽 𝑖 ) ∈ X ★(u𝑖 , ˜𝜽 𝑖 ), (8) where ˜ 𝜽 𝑖 ∼ 𝑝 (· | 𝜽 ∗). Figure 1b shows a graphical model depicting this data-generating process. Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

8θ*           

> −1 01
> −1 01
> θ1
> θ2
> Linear Objective
> (a) Objective space. y1
> y2
> y3
> x1
> x2
> x3
> x2
> x1
> Linear Constraints
> y1
> y2y3
> x1
> x2
> x3
> x2
> x1
> Quadratic Constraints
> Instance 123Solution Type Observed decision
> Unobserved optimum (b) Decision space.
> Figure 2 (a) Unobserved parameter vector 𝜽 ∗. (b) Unobserved optimal solutions and their observed perturbations.

EXAMPLE 2. The setup is the same as in Example 1. The left panel in Figure 3 illustrates the perturbed parameter vectors ˜ 𝜽 1, ˜ 𝜽 2, ˜ 𝜽 3, which are generated from a von Mises-Fisher distribution (see EC.1) centered at 𝜽 ∗. The right panel shows the optimal solutions corresponding to ˜ 𝜽 𝑖 for 

𝑖 = 1, 2, 3. In contrast to Example 1, all observations lie on the boundary of the feasible region. In both error processes, when X★(u, 𝜽 ) is not a singleton, let x★(u, 𝜽 ) be chosen from this set under some rule independent of 𝜽 ∗; we discuss implications of non-uniqueness in Section 5.4. 

5. Inverse Optimization via Bayesian Inference 

In this section, we develop a new framework for data-driven IO using hierarchical Bayesian methods, inspired by the likelihood-based regression approach outlined in Section 3. 

5.1. Inverse Optimization as a Hierarchical Bayesian Model 

Both data-generating processes from Section 4.2 can be viewed as a hierarchical Bayesian model, with the structure of its intermediate layer dependent on the error model. In decision-space error, the intermediate layer is the latent optimal solution of the forward problem. In objective-space error, it is the randomly perturbed parameter vector. These hierarchies are shown in Figure 1. 

5.2. Prior Specification 

Given the likelihood specifications introduced in Section 4, Bayesian inference proceeds by placing a prior distribution on the parameter vector 𝜽 and combining it with the hierarchical structure above. Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

9θ*             

> θ
> ~1
> θ
> ~2
> θ
> ~3
> −1 01
> −1 01
> θ1
> θ2
> Perturbed Linear Objectives
> (a) Objective space. y1
> y2
> y3
> x2
> x1
> Linear Constraints
> y1
> y2
> y3
> x2
> x1
> Quadratic Constraints
> Instance 123Solution Type Observed decision (b) Decision space.
> Figure 3 (a) Unobserved parameter vector 𝜽 ∗and its perturbations. (b) Observed optimal solutions due to perturbed cost vectors.

Given the assumed scale-invariance property, we restrict the support of the prior distribution 𝜋 (·) to the unit hypersphere Sℎ−1 := {𝜽 ∈ Rℎ | ∥ 𝜽 ∥2 = 1}. The uniform distribution over Sℎ−1 is the natural choice for a non-informative prior. If prior information about the direction of 𝜽 ∗ is available, a more informative prior may be used, such as the von Mises–Fisher (vMF) distribution. 

5.3. Posterior Approximation 

The posterior distribution is not available in closed form due to the likelihood specification. Thus, we approximate it using samples from a Metropolis–Hastings algorithm (Metropolis et al. 1953, Hastings 1970). The implementation depends on the assumed data-generating process. 

5.3.1. Decision-Space Error. Under decision-space error, the likelihood 𝑓 (y𝑖 | x★(u𝑖 , 𝜽 ∗)) 

depends on 𝜽 ∗ through the solution to the forward problem FO (u𝑖 , 𝜽 ∗). Each iteration therefore requires solving FO (u𝑖 , 𝜽 ∗) to evaluate the likelihood at the proposed parameter vector. Algorithm 1 describes the approach. It begins by drawing 𝜽 0 from a prior distribution 𝜋 (·) (Step 2). Then it solves the forward problem FO (u𝑖 , 𝜽 0) for each u𝑖 to obtain optimal solutions x01, . . . , x0

> 𝑁

(Step 3), and computes the corresponding log likelihood (Step 4). In iteration 𝑡 , a candidate 𝜽 𝑐 is generated from a proposal distribution 𝑞 (· | 𝜽 𝑡 −1) with support on Sℎ−1. The forward problem is solved with 𝜽 𝑐 for each instance (Step 7) and the log likelihood evaluated (Step 8). The candidate is accepted with probability 𝛼 ; otherwise the Markov chain remains at 𝜽 𝑡 −1 (Steps 9–13). Assuming Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 10

that both 𝑞 (· | 𝜽 𝑡 −1) and 𝜋 (𝜽 ) are positive on all of Sℎ−1, the resulting Markov chain is irreducible and targets the posterior distribution 𝜋 (𝜽 | D) under standard Metropolis–Hastings conditions (Hastings 1970). When the prior 𝜋 (𝜽 ) is uniform and the proposal 𝑞 is symmetric, the prior and proposal ratios in the acceptance probability equal 1. To assess convergence to the stationary distribution, we run several chains via Algorithm 1 and use the potential scale reduction factor (PSRF) (Gelman and Rubin 1992) to choose a sufficiently long warm-up period. We discard the initial 𝑇 warm iterations and use remaining samples for inference. In practice, other parameters governing the likelihood 𝑓 (e.g., the covariance matrix in a Gaussian likelihood) are generally unknown and must be estimated alongside 𝜽 . This is straightforward to do by treating these parameters as additional unknowns and sampling them jointly with 𝜽 in the same loop (Steps 5–13). We omit this from Algorithm 1 for brevity, as the extension is standard. Implementation details are given for the specific numerical experiments we perform in Section 7. 

Algorithm 1 Metropolis–Hastings Sampling for Decision-Space Error  

> 1:

Input: dataset D = {( u1, y1), . . . , (u𝑁 , y𝑁 )} , forward problem FO (u, 𝜽 ), prior distribution 

𝜋 (·) , likelihood 𝑓 (y | x★), proposal distribution 𝑞 (· | 𝜽 ), warm-up period 𝑇 warm , post warm-up iterations 𝑇  

> 2:

Draw 𝜽 0 ∼ 𝜋 (·)  

> 3:

Solve FO (u𝑖 , 𝜽 0) to obtain x★(u𝑖 , 𝜽 0) for all 𝑖 = 1, . . . , 𝑁  

> 4:

Compute log-likelihood 𝐿 𝑓 (𝜽 0; D) := Í𝑁 𝑖 =1 log 𝑓  y𝑖 | x★(u𝑖 , 𝜽 0) 

> 5:

for 𝑡 = 1, . . . , 𝑇 warm + 𝑇 do Draw candidate:  

> 6:

Draw 𝜽 𝑐 ∼ 𝑞 (· | 𝜽 𝑡 −1) 

> 7:

Solve FO (u𝑖 , 𝜽 𝑐 ) to obtain x★(u𝑖 , 𝜽 𝑐 ) for all 𝑖 = 1, . . . , 𝑁  

> 8:

Compute log-likelihood 𝐿 𝑓 (𝜽 𝑐 ; D) := Í𝑁 𝑖 =1 log 𝑓  y𝑖 | x★(u𝑖 , 𝜽 𝑐 )

Accept or reject:  

> 9:

Compute acceptance probability 𝛼 ← min 



1, exp  𝐿 𝑓 (𝜽 𝑐 ; D) − 𝐿 𝑓 (𝜽 𝑡 −1; D)  · 𝜋 (𝜽 𝑐 ) 𝑞 (𝜽 𝑡 −1 | 𝜽 𝑐 )

𝜋 (𝜽 𝑡 −1) 𝑞 (𝜽 𝑐 | 𝜽 𝑡 −1)

 

> 10:

Draw 𝑟 ∼ Uniform (0, 1) 

> 11:

𝜽 𝑡 ←



𝜽 𝑐 if 𝑟 ≤ 𝛼, 

𝜽 𝑡 −1 otherwise.  

> 12:

end for  

> 13:

return {𝜽 𝑇 warm +1, . . . , 𝜽 𝑇 warm +𝑇 }Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 11

5.3.2. Objective-Space Error. In contrast to the decision-space error model, in the objective-space error model the perturbation occurs before the forward optimization model is solved, and the observed decisions are exact solutions to FO (u𝑖 , ˜𝜽 𝑖 ). This structure suggests two-step approach: first recover the perturbed parameter vectors ˜ 𝜽 1, . . . , ˜𝜽 𝑁 from the observed decisions via inverse optimization, and then use 𝑝 ( ˜𝜽 𝑖 | ·) as the basis for inference. Algorithm 2 implements this approach. In Step 2, an inverse optimization problem is solved for each observation (u𝑖 , y𝑖 ):˜𝜽 𝑖 ∈ arg min 

> 𝜽 ∈Θ

IO (u𝑖 , y𝑖 ), 𝑖 = 1, . . . , 𝑁. (9) Formulation (9) specializes (4) to the case of a single observed decision. In the case of objective-space error, y𝑖 ∈ X ★(u𝑖 , ˜𝜽 𝑖 ) for some ˜ 𝜽 𝑖 ∈ Θ, so the optimal value of (9) is zero for any choice of loss function ℓ. When the minimizer is unique, ℓ does not affect ˜ 𝜽 𝑖 . When it is not, different choices of ℓ may select different solutions, which we discuss further below. The resulting ˜ 𝚯 = { ˜𝜽 1, . . . , ˜𝜽 𝑁 }

is fixed throughout the subsequent MCMC steps. In Steps 4 and 7, the perturbation distribution 

𝑝 ( ˜𝜽 𝑖 | 𝜽 ) replaces the likelihood 𝑓 (y𝑖 | x★(u𝑖 , 𝜽 )) as the basis for the evaluations. The remainder of the algorithm is essentially unchanged, including determining the warm-up period using PSRF. Figure EC.1 shows histograms of 10,000 MCMC samples (post-warm-up) of the angular coor-dinate of 𝜽 drawn from Algorithms 1 and 2 applied to the data in Examples 1 and 2, respectively. 

REMARK 1 (N ON -UNIQUENESS OF (9) ). When an optimal solution ˜ 𝜽 𝑖 to (9) is not unique, it lies within a set of observationally equivalent parameters, and the specific representative selected depends on the choice of loss function ℓ. In this case, Algorithm 2 targets a plug-in approximation 

𝜋 (𝜽 | { ˜𝜽 𝑖 }𝑁 𝑖 =1), based on a representative parameter vector recovered from each observation, rather than the full posterior 𝜋 (𝜽 | D) . However, the impact of this distinction diminishes asymptotically as new information accumulates through covariate variation, which we discuss in detail in Section 5.4. 

REMARK 2 (A LGORITHM COMPARISON ). Regarding optimization, Algorithm 2 has two key dif-ferences from Algorithm 1. First, no optimization problems are solved inside the MCMC loop (Steps 5–11). Second, solving the inverse problem in Step 2 requires identifying a parameter vector that rationalizes a single observation, which can be done in closed form when the objective is linear in 

𝜽 and the observation is on the boundary of the feasible region. Importantly, these differences exist because the algorithms are specialized to two different data-generating processes. The choice of algorithm should be guided by the observed data and the hypothesized data-generating mechanism. Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 12

Algorithm 2 Metropolis–Hastings Sampling for Objective-Space Error  

> 1:

Input: dataset D = {( u1, y1), . . . , (u𝑁 , y𝑁 )} , prior distribution 𝜋 (·) , perturbation distribution 

𝑝 ( ˜𝜽 | 𝜽 ), proposal distribution 𝑞 (· | 𝜽 ), warm-up period 𝑇 warm , post warm-up iterations 𝑇  

> 2:

Solve IO (u𝑖 , y𝑖 ) for all 𝑖 = 1, . . . , 𝑁 to obtain ˜ 𝚯 = { ˜𝜽 1, . . . , ˜𝜽 𝑁 } 

> 3:

Draw 𝜽 0 ∼ 𝜋 (·)  

> 4:

Compute 𝐿 𝑝 (𝜽 0; ˜ 𝚯 ) := Í𝑁 𝑖 =1 log 𝑝 ( ˜𝜽 𝑖 | 𝜽 0) 

> 5:

for 𝑡 = 1, . . . , 𝑇 warm + 𝑇 do Draw candidate:  

> 6:

Draw 𝜽 𝑐 ∼ 𝑞 (· | 𝜽 𝑡 −1) 

> 7:

Compute 𝐿 𝑝 (𝜽 𝑐 ; ˜ 𝚯 ) := Í𝑁 𝑖 =1 log 𝑝 ( ˜𝜽 𝑖 | 𝜽 𝑐 )

Accept or reject:  

> 8:

Compute acceptance probability 𝛼 ← min 



1, exp  𝐿 𝑝 (𝜽 𝑐 ; ˜ 𝚯 ) − 𝐿 𝑝 (𝜽 𝑡 −1; ˜ 𝚯 ) · 𝜋 (𝜽 𝑐 ) 𝑞 (𝜽 𝑡 −1 | 𝜽 𝑐 )

𝜋 (𝜽 𝑡 −1) 𝑞 (𝜽 𝑐 | 𝜽 𝑡 −1)

 

> 9:

Draw 𝑟 ∼ Uniform (0, 1) 

> 10:

𝜽 𝑡 ←



𝜽 𝑐 if 𝑟 ≤ 𝛼, 

𝜽 𝑡 −1 otherwise.  

> 11:

end for  

> 12:

return {𝜽 𝑇 warm +1, . . . , 𝜽 𝑇 warm +𝑇 }

5.4. Identifiability and Consistency 

Here we characterize the conditions under which the parameter vector 𝜽 ∗ is identifiable from the observed data and the posterior distribution is consistent as 𝑁 → ∞ .

DEFINITION 1 (I DENTIFIABILITY ). The parameter 𝜽 ∗ ∈ Θ is said to be identifiable if 

ℎ(· | 𝜽 ) = ℎ(· | 𝜽 ∗) =⇒ 𝜽 = 𝜽 ∗,

where ℎ(y | 𝜽 ) is the data-generating distribution of the observations y under parameter 𝜽 .In standard parametric models, identifiability is determined by the likelihood, and posterior consistency follows under mild regularity conditions on the model and prior (Van der Vaart 2000). In inverse optimization, the mapping from 𝜽 to observed data is mediated by an optimization problem, and whether distinct parameters induce distinct data-generating distributions depends on the structure of the forward problem; e.g., when the mapping 𝜽 ↦ → x★(u, 𝜽 ) is one-to-one, classical results apply directly. Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 13

For each u ∈ U , define the instance-specific equivalence class 

Θ∗ (u) := 𝜽 ∈ Θ x★(u, 𝜽 ) = x★(u, 𝜽 ∗) ,

which contains all parameter vectors that generate the same optimal solution as 𝜽 ∗ for covariate u.When Θ∗ (u) is not a singleton, the covariate u alone cannot distinguish 𝜽 ∗ from other elements of 

Θ∗ (u), regardless of the number of observations at that covariate value. Across multiple observations with distinct covariates, however, identifying information on 𝜽 ∗

accumulates when the covariates induce different optimal solutions for different parameter vectors. Given a dataset D = {( u𝑖 , y𝑖 )} 𝑁 𝑖 =1, define the globally identified set 

Θ∗ 

> 𝑁

:=

> 𝑁

Ù

> 𝑖 =1

Θ∗ (u𝑖 ).

Even if each Θ∗ (u𝑖 ) is large, Θ∗ 

> 𝑁

may shrink rapidly as 𝑁 increases. Lemma 1 below formalizes conditions under which the set Θ∗ 

> 𝑁

converges to the singleton {𝜽 ∗}.

LEMMA 1 (Identification) . Let u𝑖 ∼ PU be i.i.d. and Θ be compact. Assume that for every 𝜽 ≠ 𝜽 ∗,there exists an open neighborhood O𝜽 of 𝜽 and a measurable set B𝜽 ⊆ U with PU (B 𝜽 ) > 0 such that x★(u, 𝜽 ′) ≠ x★(u, 𝜽 ∗) for all 𝜽 ′ ∈ O 𝜽 and u ∈ B 𝜽 . Then Θ∗ 

> 𝑁

→ { 𝜽 ∗} almost surely as 𝑁 → ∞ .Proof. Let (V 𝑘 )∞ 

> 𝑘 =1

be a decreasing sequence (i.e., V𝑘 +1 ⊆ V 𝑘 ) of open neighborhoods of 𝜽 ∗

such that Ñ∞ 

> 𝑘 =1

V𝑘 = {𝜽 ∗}. Fix 𝑘 . Since Θ is compact and V𝑘 is open, the set Θ \ V 𝑘 is compact. The collection {O 𝜽 | 𝜽 ∈ Θ \ V 𝑘 } is an open cover of Θ \ V 𝑘 , so by compactness there exists a finite subcover {O 𝜽 1 , . . . , O𝜽 𝑚 } with corresponding covariate sets B𝜽 1 , . . . , B𝜽 𝑚 .For each 𝑗 ∈ { 1, . . . , 𝑚 }, since PU (B 𝜽 𝑗 ) > 0 and the u𝑖 are i.i.d., the Borel-Cantelli lemma implies that, almost surely, there is some 𝑖 𝑗 such that u𝑖 𝑗 ∈ B 𝜽 𝑗 . For this u𝑖 𝑗 , there is no 𝜽 ′ ∈ O 𝜽 𝑗 that lies in 

Θ∗ (u𝑖 𝑗 ). Since Θ∗ 

> 𝑁 +1

⊆ Θ∗ 

> 𝑁

, it follows that Θ∗ 

> 𝑁

∩ O 𝜽 𝑗 = ∅ for all 𝑁 ≥ 𝑖 𝑗 .Therefore, for all 𝑁 ≥ max 1≤ 𝑗 ≤𝑚 𝑖 𝑗 , Θ∗ 

> 𝑁

⊆ Θ \ ∪ 𝑚 𝑗 =1O𝜽 𝑗 ⊆ V 𝑘 . Thus, for this fixed 𝑘 , Θ∗ 

> 𝑁

⊆ V 𝑘 

almost surely for all sufficiently large 𝑁 . Taking a countable intersection over 𝑘 , we obtain that almost surely, for every 𝑘 , Θ∗ 

> 𝑁

⊆ V 𝑘 eventually. Since Ñ𝑘 V𝑘 = {𝜽 ∗}, Θ∗ 

> 𝑁

→ { 𝜽 ∗} almost surely. □

Lemma 1 shows that covariate variation yields asymptotic identification of 𝜽 ∗. The next result establishes that under suitable conditions, this identification is reflected in the posterior distribution as well. Before stating the result, note that both error processes from Section 4.2 can be expressed using a common representation for the observation density conditional on the optimal solution Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 14

given 𝜽 , denoted 𝜈 (y | x★(u, 𝜽 )) . This provides a convenient abstraction for the analysis below. For decision-space error, set 𝜈 = 𝑓 . For objective-space error, 

𝜈 (y | x★(u, 𝜽 )) =

∫

{ ˜𝜽 ∈Θ | x★ (u, ˜𝜽 )=y} 𝑝 ( ˜𝜽 | 𝜽 ) 𝑑 ˜𝜽 . (10) This likelihood is determined by a unique perturbed parameter vector when each ˜ 𝜽 generates a distinct x★(u, ˜𝜽 ).

THEOREM 1 (Posterior consistency) . Let D𝑁 = {( u𝑖 , y𝑖 )} 𝑁 𝑖 =1 be i.i.d. samples, where u𝑖 ∼ PU and 

y𝑖 | u𝑖 ∼ 𝜈 (· | x★(u𝑖 , 𝜽 ∗)) . Let 𝜋 (𝜽 ) be the prior distribution, 𝜈 (· | x★(u, 𝜽 )) be the likelihood function, and 𝜋 (𝜽 | D 𝑁 ) be the posterior obtained via Bayesian updating according to (1) . Let 𝐷 (· ∥ ·) be the Kullback–Leibler (KL) divergence and U ( 𝜽 ) := u ∈ U | x★(u, 𝜽 ) ≠ x★(u, 𝜽 ∗) . Assume: (A1) (Covariate variation) For all 𝜽 ≠ 𝜽 ∗, PU (U ( 𝜽 )) > 0.(A2) (Observation model) For all x ≠ x′, the conditional densities satisfy 𝜈 (· | x) ≠ 𝜈 (· | x′) and are defined with respect to the same underlying measure. (A3) (Regularity at 𝜽 ∗) For all u (except possibly on a PU -measure zero set), x★(u, 𝜽 ) is continuous in 𝜽 at 𝜽 ∗ and 𝜈 (· | x) is continuous in x. Furthermore, there exists a PU -integrable function 

𝜁 : U → R+ and a neighborhood of 𝜽 ∗ on which 𝐷  𝜈 (· | x★(u, 𝜽 ∗)) 𝜈 (· | x★(u, 𝜽 ))  ≤ 𝜁 (u).(A4) (Prior support) The prior 𝜋 (𝜽 ) assigns positive mass to every neighborhood of 𝜽 ∗.Then the posterior satisfies 𝜋 (𝜽 ∈ A | D 𝑁 ) → 1 almost surely for every open set A containing 𝜽 ∗.Proof. Let 𝐾 (𝜽 ∗, 𝜽 ) := ∫ 

> U

𝐷  𝜈 (· | x★(u, 𝜽 ∗)) 𝜈 (· | x★(u, 𝜽 ))  𝑑 PU . For any 𝜽 ≠ 𝜽 ∗ and any 

u ∈ U ( 𝜽 ), 𝜈 (· | x★(u, 𝜽 )) ≠ 𝜈 (· | x★(u, 𝜽 ∗)) by (A2), so 𝐷  𝜈 (· | x★(u, 𝜽 ∗)) 𝜈 (· | x★(u, 𝜽 ))  > 0. Since U ( 𝜽 ) has positive PU -measure by (A1), it follows that 𝐾 (𝜽 ∗, 𝜽 ) > 0 for all 𝜽 ≠ 𝜽 ∗.Next we show that 𝐾 (𝜽 ∗, ·) is continuous at 𝜽 ∗. By (A3), for almost every u, x★(u, 𝜽 ) → x★(u, 𝜽 ∗)

as 𝜽 → 𝜽 ∗, and so 𝜈 (· | x★(u, 𝜽 )) → 𝜈 (· | x★(u, 𝜽 ∗)) . Hence, 𝐷  𝜈 (· | x★(u, 𝜽 ∗)) 𝜈 (· | x★(u, 𝜽 ))  → 0pointwise as 𝜽 → 𝜽 ∗. Combined with the integrability bound in (A3), the dominated convergence theorem implies 𝐾 (𝜽 ∗, 𝜽 ) → 0 as 𝜽 → 𝜽 ∗, i.e., 𝐾 (𝜽 ∗, ·) is continuous at 𝜽 ∗.To complete the proof, we require Schwartz’s theorem (Schwartz 1965), which says 𝜋 (𝜽 ∈ A | D𝑁 ) → 1 as 𝑁 → ∞ for every open set A that contains 𝜽 ∗ if (i) the parameter space is compact, (ii) the model is identifiable, and (iii) there is positive prior mass on every KL neighborhood of 𝜽 ∗, i.e., for all 𝜖 > 0, 𝜋 (A KL (𝜽 ∗, 𝜖 )) > 0, where AKL (𝜽 ∗, 𝜖 ) := {𝜽 ∈ Θ | 𝐾 (𝜽 ∗, 𝜽 ) < 𝜖 }. The first condition is met since Θ is compact. The second is implied by (A1) and (A2). For the third condition, since 

𝐾 (𝜽 ∗, ·) is continuous at 𝜽 ∗ and 𝐾 (𝜽 ∗, 𝜽 ∗) = 0, AKL (𝜽 ∗, 𝜖 ) contains an open neighborhood of 𝜽 ∗. By (A4), the prior therefore assigns positive mass to AKL (𝜽 ∗, 𝜖 ) for every 𝜖 > 0. The result follows. □Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 15

REMARK 3 (T HEOREM 1 A SSUMPTIONS ). Assumption (A1) requires that for any parameter 

𝜽 ≠ 𝜽 ∗, the dataset is sufficiently rich that there exists an instance u𝑖 such that the optimal solution with respect to 𝜽 differs from that with respect to 𝜽 ∗. Without (A1), Θ∗ 

> 𝑁

may not collapse to a singleton. Assumption (A2) requires the observation model be sufficiently informative to distinguish different optimal decisions. This holds, for example, with a Gaussian noise model such as in Example 1. Assumption (A3) holds for the Gaussian and vMF observation models considered in this paper. In both cases, 𝜈 (· | x) is continuous in x, and 𝜽 ↦ → x★(u, 𝜽 ) is continuous at 𝜽 ∗ for almost every u. Moreover, the corresponding KL divergence is uniformly bounded under compactness of X( u) (Gaussian case) and on the unit sphere (vMF case). Assumption (A4) is standard in the Bayesian inference literature and is satisfied by priors with full support on Θ such as the uniform distribution. 

REMARK 4 (R ELATIONSHIP TO LEMMA 1). The assumption in Lemma 1 strengthens the requirement that each 𝜽 ≠ 𝜽 ∗ be distinguishable from 𝜽 ∗ on a positive-measure covariate set, by requiring that the distinguishability hold locally uniformly in a neighborhood of 𝜽 . This is natural when x★(u, ·) is continuous in 𝜽 and distinct parameter values induce separated optimal solutions on positive-measure sets of covariates, as is typical in well-identified strictly convex forward problems. Note that this strengthened condition implies (A1) of Theorem 1; the additional local uniformity is required only for the set-convergence result and not for posterior consistency. 

REMARK 5 (A DDITIONAL PARAMETERS ). Theorem 1 focuses on consistency of 𝜽 . Often, addi-tional parameters are estimated jointly with 𝜽 (see Section 7). Extending the consistency result in this case follows standard arguments for finite-dimensional parametric models and is omitted for brevity. Theorem 1 establishes that the posterior concentrates around 𝜽 ∗ as the number of observations increases. The proof follows the standard structure of posterior consistency results in Bayesian inference, but is suitably adapted to incorporate the optimization layer in our hierarchical Bayesian model: the identifiability condition (A1) is expressed in terms of covariate variation rather than direct distinguishability of the likelihood, and the KL divergence is integrated over the covariate distribution to account for the role of the optimization in linking parameters to observations. 

6. Uncertainty Quantification 

The posterior distribution of 𝜽 characterizes the uncertainty in the estimated parameter vector. Since inference is restricted to the unit hypersphere Sℎ−1, uncertainty is naturally measured in terms Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

16 

of angular (geodesic) distance. To construct geometrically relevant credible regions, we adopt a tangent space approximation based on tools from directional statistics and Riemannian geometry (see, e.g., Mardia and Jupp (2009) and Absil et al. (2008)). 

6.1. Constructing Credible Regions 

Assume {𝜽 1, . . . , 𝜽 𝑇 } is the set of (post warm-up) posterior samples from Algorithm 1 or Algo-rithm 2. The construction proceeds in three steps (illustrated in Figure 4): (a) compute a posterior mean direction, (b) map posterior samples to the tangent space via the logarithmic map, and (c) con-struct an ellipsoidal credible region in the tangent space. This mapping follows standard definitions from Riemannian geometry (Absil et al. 2008). 

6.1.1. Posterior Mean Direction. The posterior mean direction is calculated as ˆ𝜽 :=

> 1
> 𝑇

Í𝑇 𝑡 =1 𝜽 𝑡 

> 1
> 𝑇

Í𝑇 𝑡 =1 𝜽 𝑡 2

.

It represents the central direction of the posterior distribution. x1

> 1.0
> 0.5
> 0.0
> 0.5
> 1.0

x2

> 1.0
> 0.5
> 0.0
> 0.5
> 1.0

x3 0.5   

> 1.0
> Figure 4 Construction of the credible region on the hypersphere. Posterior samples (blue dots) are mapped to points (orange dots) in the hyperplane tangent to the hypersphere at the mean direction (red arrow). The ellipsoidal region is generated in the tangent space (dashed curve) and can be mapped back to the hypersphere (solid curve). Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO
> 17

6.1.2. Mapping to Tangent Space. Let Δ ˆ𝜽 = {v ∈ Rℎ | ( v − ˆ𝜽 )⊤ ˆ𝜽 = 0} denote the affine tangent space to Sℎ−1 at ˆ 𝜽 . We map each posterior sample 𝜽 𝑡 to a point 𝜼 𝑡 ∈ Δ ˆ𝜽 via 𝜼 𝑡 = log ˆ𝜽 (𝜽 𝑡 ), where log ˆ𝜽 (𝜽 ) := ˆ𝜽 + 𝜽 − ( ˆ𝜽 ⊤𝜽 ) ˆ𝜽 

∥𝜽 − ( ˆ𝜽 ⊤𝜽 ) ˆ𝜽 ∥2

arccos ( ˆ𝜽 ⊤𝜽 ).

The Euclidean distance ∥𝜼 𝑡 − ˆ𝜽 ∥2 in the tangent space equals the geodesic (angular) distance between 𝜽 𝑡 and ˆ 𝜽 . When posterior mass is sufficiently concentrated (as expected under posterior consistency; see Theorem 1), geodesic distances between points on the hypersphere will be well-approximated by Euclidean distances of the corresponding points in the tangent space. 

6.1.3. Ellipsoidal Credible Region. Let the empirical mean and covariance matrix of the points in the tangent space be ¯ 𝜼 = 1

> 𝑇

Í𝑇 𝑡 =1 𝜼 𝑡 and 𝚺 = 1

> 𝑇

Í𝑇 𝑡 =1

 𝜼 𝑡 − ¯𝜼    𝜼 𝑡 − ¯𝜼 ⊤. Define the squared Mahalanobis distances that measure how far each posterior sample is from the mean direction, accounting for the empirical covariance, by 𝑑 𝑡 :=  𝜼 𝑡 − ¯𝜼 ⊤ 𝚺 −1  𝜼 𝑡 − ¯𝜼 , 𝑡 = 1, . . . , 𝑇 . 

Let 𝑞 1−𝛼 denote the empirical (1 − 𝛼 )-quantile of {𝑑 1, . . . 𝑑 𝑇 }. The corresponding ellipsoid 

𝐸 1−𝛼 = 𝜼 ∈ Δ ˆ𝜽 (𝜼 − ¯𝜼 )⊤𝚺 −1 (𝜼 − ¯𝜼 ) ≤ 𝑞 1−𝛼 

summarizes posterior variability in the tangent space. The credible region on the hypersphere is therefore 𝐶 1−𝛼 = 𝜽 ∈ S ℎ−1 log ˆ𝜽 (𝜽 ) ∈ 𝐸 1−𝛼 . Geometrically, 𝐶 1−𝛼 forms an anisotropic region on the hypersphere, with shape determined by the posterior covariance structure. Note that 𝐸 1−𝛼 is centered at ¯ 𝜼 , which is generally not equal to ˆ 𝜽 , but this difference vanishes as 𝑁 → ∞ and 𝑇 → ∞ .

REMARK 6. Since 𝜼 𝑡 , 𝑡 = 1, . . . , 𝑇 lie in the (ℎ − 1)-dimensional affine subspace Δ ˆ𝜽 , the empirical covariance 𝚺 is rank-deficient and its inverse does not exist. We therefore regularize 𝚺 by adding a small constant 𝜖 > 0 along the diagonal, so that it becomes 𝚺 + 𝜖 I, where I is the identity matrix. For sufficiently small 𝜖 , this regularization has negligible impact relative to posterior uncertainty in the numerical experiments. 

6.2. Scalar Summary of Posterior Dispersion 

Next, we propose an interpretable summary of posterior dispersion. Let 𝜽 ∼ 𝜋 (· | D 𝑁 ) and 𝜼 =

log ˆ𝜽 (𝜽 ), where ˆ 𝜽 := E[𝜽 | D 𝑁 ]/∥ E[𝜽 | D 𝑁 ] ∥ 2. Let 𝚺 𝑝 := Cov [𝜼 | D 𝑁 ] denote the posterior covari-ance of 𝜼 . Let tr (A) denote the trace of matrix A. We can compactly summarize posterior dispersion using the root mean squared angular deviation of 𝜽 from the posterior mean direction ˆ 𝜽 :

𝛼 RMS =√︂ tr (𝚺 𝑝 )

ℎ − 1 .Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 18

PROPOSITION 1. Under the assumptions of Theorem 1, 𝛼 RMS → 0 as 𝑁 → ∞ .Proof. Since 𝜼 = log ˆ𝜽 (𝜽 ), ∥𝜼 − ˆ𝜽 ∥2 = arccos ( ˆ𝜽 ⊤𝜽 ). By Theorem 1, 𝜽 → 𝜽 ∗ in probability as 

𝑁 → ∞ , so ˆ 𝜽 → 𝜽 ∗ and ∥𝜼 − ˆ𝜽 ∥2 → 0 in probability as 𝑁 → ∞ . Since arccos (·) ≤ 𝜋 , ∥𝜼 − ˆ𝜽 ∥2 is uniformly bounded above for all 𝜽 ∈ S ℎ−1 and all 𝑁 . This implies uniform integrability, and so convergence in probability implies convergence of second moments: E∥𝜼 − ˆ𝜽 ∥22 D𝑁 

 → 0. The trace of 𝚺 𝑝 equals the sum of the variances along the diagonal, tr (𝚺 𝑝 ) = E∥𝜼 − E[𝜼 | D 𝑁 ] ∥ 22 D𝑁 



≤ E∥𝜼 − ˆ𝜽 ∥22 D𝑁 

,

where the inequality follows from the fact that the quantity that minimizes the mean squared distance from 𝜼 is its expectation. Hence, tr (𝚺 𝑝 ) → 0 as 𝑁 → ∞ , implying 𝛼 RMS → 0. □

In practice, 𝛼 RMS is computed using the empirical covariance of MCMC samples in place of the posterior covariance 𝚺 𝑝 . This empirical covariance converges almost surely to 𝚺 𝑝 as 𝑇 → ∞ 

(Robert and Casella 2004), so the empirical 𝛼 RMS inherits the same asymptotic behavior as 𝛼 RMS 

defined by 𝚺 𝑝 . We report the empirical 𝛼 RMS in our numerical experiments in Section 7. 

7. Numerical Experiments 

We illustrate the application of our algorithms to estimate credible regions in linearly and quadrat-ically constrained problems with a linear objective. To distinguish them, we will refer to them as LP and QP, respectively. The focus is on empirically assessing coverage of the credible regions. 

7.1. Simulation Setup 

Details on the optimization model formulations and the corresponding simulated datasets are given in EC.3. The data-generating and inference procedures are repeated 100 times. Coverage is defined as the proportion of those simulations in which the true parameter vector 𝜽 ∗ lies within the constructed credible region. All credible regions are constructed at the 95% ( 𝛼 = 0.05) level following the approach from Section 6. In Algorithm 1, we assume the likelihood 𝑓 is Gaussian with mean x★(u, 𝜽 ∗) and unknown covariance matrix 𝜎 2I. We place a uniform prior on 𝜽 and a half-Cauchy prior on 𝜎 (Gelman et al. 2013). We update 𝜽 and 𝜎 within each MCMC iteration separately using Gaussian random walks. In Algorithm 2, we assume a von Mises-Fisher perturbation distribution 𝑝 centered at 𝜽 ∗

with unknown concentration parameter 𝜅 . We place a uniform prior on 𝜽 and a Gamma prior on Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 19

𝜅 , and update them using a Gaussian random walk for 𝜽 and a log-normal proposal for 𝜅 . The IO problems are solved as outlined in EC.3.3. For both algorithms, we choose a Gaussian proposal distribution 𝑞 because it is easy to sample from in any dimension and the covariance matrix can be tuned to match the shape and scale of the posterior distribution, which is important for efficient mixing in high dimensions. Additional details regarding the proposal distribution including alternatives considered are provided in EC.3.4. We run each chain until the PSRF falls below 1.1 across all parameters; in our experiments, this required between 10,000 and 100,000 iterations per chain. A warm-up period of 25% of the total iterations is discarded in all cases. The reported times are the wall-clock durations of one run of each algorithm on a single processor, including the warm-up iterations. 

7.2. Results 

Table 1 summarizes the empirical coverage percentages and the mean 𝛼 RMS (± 1 standard deviation) across 100 replications, obtained by applying each algorithm to an LP model with data generated under its corresponding error model. We vary the number of observed decisions ( 𝑁 ), the variance (𝜎 2) of the Gaussian distribution in Algorithm 1, and the concentration parameter ( 𝜅 ) of the vMF distribution (see EC.1) in Algorithm 2. Table 2 reports the analogous results for the QP. Both tables show that empirical coverage is near the nominal 95% level for all 𝑁 , 𝜎 and 𝜅 . Both the mean and standard deviation of 𝛼 RMS are largest for small 𝑁 . These values are particularly large in the LP case because of the relatively few data points to resolve the nonidentifiability. In all cases, the size and variability of the credible region decreases with increasing 𝑁 , as expected. The credible regions for the QP tend to be smaller, particularly for small 𝑁 , reflecting the strict convexity of the QP feasible region, which ensures that each observation provides precise identifying information about the true parameter vector. In the LP case, since multiple parameter vectors can generate the same optimal solution, residual directional uncertainty manifests through larger credible regions. Second, the standard deviation of 𝛼 RMS is markedly smaller and more stable across replications for the QP than for the LP, particularly under Algorithm 1. Since the QP is identifiable, the posterior concentrates consistently across replications, yielding stable credible region sizes. For the LP, the directions that remain uncertain vary across replications depending on the realized covariate set, inflating variability. Finally, we find that Algorithm 1 is more computationally demanding than Algorithm 2, as expected. The computation times for Algorithm 1 grow approximately linearly with 𝑁 , whereas for Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

20  

> Table 1 Applying Algorithms 1 and 2 to LP instances.

Algorithm 1 Algorithm 2 

𝑁 𝜎 2 Coverage (%) 𝛼 RMS (deg) Time (s) 𝜅 Coverage (%) 𝛼 RMS (deg) Time (s) 50 0.05 95 42.61 ± 13.42 1703 10 97 39.96 ± 10.92 161 100 96 26.40 ± 9.16 3771 99 26.36 ± 4.47 168 200 94 14.29 ± 4.12 8542 97 17.82 ± 2.01 330 500 97 5.95 ± 2.04 36,941 93 10.88 ± 0.83 449 1000 95 3.34 ± 1.48 81,281 94 7.75 ± 0.37 653 50 0.1 94 54.16 ± 16.29 1517 5 96 52.16 ± 17.58 164 100 96 32.47 ± 10.82 3627 95 33.05 ± 8.50 166 200 93 19.01 ± 5.95 8044 94 22.71 ± 3.78 230 500 96 8.58 ± 2.67 37,349 97 13.94 ± 1.25 368 1000 94 4.75 ± 1.42 39,399 98 9.99 ± 0.72 554  

> Table 2 Applying Algorithms 1 and 2 to QP instances.

Algorithm 1 Algorithm 2 

𝑁 𝜎 2 Coverage (%) 𝛼 RMS (deg) Time (s) 𝜅 Coverage (%) 𝛼 RMS (deg) Time (s) 50 0.05 97 11.01 ± 0.31 499 10 97 21.59 ± 1.60 234 100 93 8.60 ± 0.31 903 96 15.55 ± 0.89 254 200 95 6.40 ± 0.27 2559 95 11.13 ± 0.37 278 500 95 5.24 ± 0.27 6604 97 7.34 ± 0.18 307 1000 96 3.67 ± 0.25 23,435 97 5.61 ± 0.13 355 50 0.1 95 14.58 ± 0.49 487 5 94 39.94 ± 5.67 206 100 95 10.88 ± 0.32 863 97 28.25 ± 2.79 220 200 97 8.01 ± 0.25 2460 94 20.24 ± 1.40 244 500 95 6.07 ± 0.25 6450 96 13.04 ± 0.50 299 1000 97 4.73 ± 0.22 20,102 96 9.42 ± 0.27 329 

Algorithm 2, they grow much more slowly with 𝑁 (particularly for the QP). The result is about two orders of magnitude less time for the largest 𝑁 values we considered. 

8. Conclusions 

This paper develops a probabilistic modeling framework for inverse optimization that enables uncertainty quantification of a vector that parameterizes a linear cost function. Using Bayesian inference, we construct credible regions that are both geometrically consistent and computationally tractable. Under appropriate identifiability conditions, we show that the posterior concentrates around the true parameter. We further demonstrate that the resulting credible regions achieve near-nominal empirical coverage. This framework provides a principled way to propagate uncertainty into downstream optimization models or calibrate a decision maker’s confidence in IO-based inferences. 

References 

Absil PA, Mahony R, Sepulchre R (2008) Optimization algorithms on matrix manifolds (Princeton University Press). Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

21 Ahuja RK, Orlin JB (2001) Inverse optimization. Operations research 49(5):771–783. Ajayi T, Lee T, Schaefer AJ (2022) Objective selection for cancer treatment: An inverse optimization approach. 

Operations Research 70(3):1717–1738. Aswani A, Shen ZJ, Siddiq A (2018) Inverse optimization with noisy data. Operations Research 66(3):870–892. Babier A, Chan TC, Lee T, Mahmood R, Terekhov D (2021) An ensemble learning framework for model fitting and evaluation in inverse linear optimization. Informs Journal on Optimization 3(2):119–138. Bertsimas D, Gupta V, Paschalidis IC (2015) Data-driven estimation in equilibrium using inverse optimization. Math-ematical Programming 153(2):595–633. Besbes O, Fonseca Y, Lobel I (2025) Contextual inverse optimization: Offline and online learning. Operations Research 

73(1):424–443. Birge JR, Li X, Sun C (2022) Stochastic inverse optimization. Technical report, Working paper. Bodur M, Chan TC, Zhu IY (2022) Inverse mixed integer optimization: Polyhedral insights and trust region methods. 

INFORMS Journal on Computing 34(3):1471–1488. Bulut A, Ralphs TK (2021) On the complexity of inverse mixed integer linear optimization. SIAM Journal on Opti-mization 31(4):3014–3043. Chan T, Delage E, Lin B (2024) Conformal inverse optimization for adherence-aware prescriptive analytics. Available at SSRN 4970102 .Chan TC, Craig T, Lee T, Sharpe MB (2014) Generalized inverse multiobjective optimization with application to cancer therapy. Operations Research 62(3):680–695. Chan TC, Eberg M, Forster K, Holloway C, Ieraci L, Shalaby Y, Yousefi N (2022a) An inverse optimization approach to measuring clinical pathway concordance. Management Science 68(3):1882–1903. Chan TC, Forster K, Habbous S, Holloway C, Ieraci L, Shalaby Y, Yousefi N (2022b) Inverse optimization on hierarchical networks: an application to breast cancer clinical pathways. Health Care Management Science 

25(4):590–622. Chan TC, Lee T, Terekhov D (2019) Inverse optimization: Closed-form solutions, geometry, and goodness of fit. 

Management Science 65(3):1115–1135. Chan TC, Mahmood R, Zhu IY (2025) Inverse optimization: Theory and applications. Operations Research 73(2):1046– 1074. Chow JY, Ritchie SG, Jeong K (2014) Nonlinear inverse optimization for parameter estimation of commodity-vehicle-decoupled freight assignment. Transportation Research Part E: Logistics and Transportation Review 67:71–91. Esfahani PM, Shafieezadeh-Abadeh S, Hanasusanto GA, Kuhn D (2018) Data-driven inverse optimization with imper-fect information. Mathematical Programming 167(1):191–234. Gelman A, Carlin JB, Stern HS, Dunson DB, Vehtari A, Rubin DB (2013) Bayesian data analysis (CRC press). Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

22 Gelman A, Hill J (2007) Data analysis using regression and multilevel/hierarchical models (Cambridge university press). Gelman A, Rubin DB (1992) Inference from iterative simulation using multiple sequences. Statistical science 7(4):457– 472. Gupta R, Zhang Q (2022) Decomposition and adaptive sampling for data-driven inverse linear optimization. INFORMS Journal on Computing 34(5):2720–2735. Gurobi Optimization, LLC (2026) Gurobi Optimizer Reference Manual. URL https://www.gurobi.com .Haario H, Saksman E, Tamminen J (2001) An adaptive Metropolis algorithm. Bernoulli 7(2):223–242. Hastings WK (1970) Monte Carlo sampling methods using Markov chains and their applications. Biometrika 57(1):97– 109. Iyengar G, Kang W (2005) Inverse conic programming with applications. Operations Research Letters 33(3):319–330. Keshavarz A, Wang Y, Boyd S (2011) Imputing a convex objective function. 2011 IEEE international symposium on intelligent control , 613–619 (IEEE). Lin B, Delage E, Chan T (2024) Conformal inverse optimization. Advances in Neural Information Processing Systems 

37:63534–63564. Lu YA, Hu WS, Paulson JA, Zhang Q (2025) Bo4io: A bayesian optimization approach to inverse optimization with uncertainty quantification. Computers & Chemical Engineering 192:108859. Mardia KV, Jupp PE (2009) Directional statistics (John Wiley & Sons). Metropolis N, Rosenbluth AW, Rosenbluth MN, Teller AH, Teller E (1953) Equation of state calculations by fast computing machines. The journal of chemical physics 21(6):1087–1092. Ren K, Esfahani PM, Georghiou A (2025) Inverse optimization via learning feasible regions. arXiv preprint arXiv:2505.15025 .Robert CP, Casella G (2004) Monte Carlo Statistical Methods (New York: Springer), 2nd edition. R¨ onnqvist M, Svenson G, Flisberg P, J¨ onsson LE (2017) Calibrated route finder: Improving the safety, environmental consciousness, and cost effectiveness of truck routing in sweden. Interfaces 47(5):372–395. Sandholtz N, Miyamoto Y, Bornn L, Smith MA (2023) Inverse bayesian optimization: Learning human acquisition functions in an exploration vs exploitation search task. Bayesian Analysis 18(1):1–24. Schaefer AJ (2009) Inverse integer programming. Optimization Letters 3:483–489. Schwartz L (1965) On bayes procedures. Zeitschrift f¨ ur Wahrscheinlichkeitstheorie und verwandte Gebiete 4(1):10–26. Shahmoradi Z, Lee T (2022) Quantile inverse optimization: Improving stability in inverse linear programming. Oper-ations research 70(4):2538–2562. Van der Vaart AW (2000) Asymptotic statistics , volume 3 (Cambridge university press). Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

23 Wang L (2009) Cutting plane algorithms for the inverse mixed integer linear programming problem. Operations research letters 37(2):114–116. Zattoni Scroccaro P, Atasoy B, Mohajerin Esfahani P (2025a) Learning in inverse optimization: Incenter cost, augmented suboptimality loss, and algorithms. Operations Research 73(5):2661–2679. Zattoni Scroccaro P, van Beek P, Mohajerin Esfahani P, Atasoy B (2025b) Inverse optimization for routing problems. 

Transportation Science 59(2):301–321. Zhang J, Xu C (2010) Inverse optimization for linearly constrained convex separable programming problems. European Journal of Operational Research 200(3):671–679. 

Endnotes 

> 1

Throughout the paper, we use the superscript ★ to denote optimal solutions of an optimization problem (e.g., x★ (u, 𝜽 )), and the superscript ∗ to denote true but unknown parameter values (e.g., 𝜽 ∗). Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 24

## Electronic Companion 

EC.1. von Mises–Fisher Distribution 

The von Mises–Fisher (vMF) distribution is a probability distribution defined on the unit hyper-sphere Sℎ−1 = {𝜽 ∈ Rℎ | ∥ 𝜽 ∥2 = 1} and is commonly used to model directional data (Mardia and Jupp 2009). It can be viewed as the analogue of the multivariate normal distribution for unit-norm vectors. A random vector ˜ 𝜽 ∈ S ℎ−1 follows a vMF distribution with mean direction 𝜽 ∈ S ℎ−1 and con-centration parameter 𝜅 ≥ 0, denoted by ˜ 𝜽 ∼ vMF (𝜽 , 𝜅 ), if its density is given by 

𝑝 ( ˜𝜽 | 𝜽 , 𝜅 ) = 𝐶 ℎ (𝜅 ) exp  𝜅 𝜽 ⊤ ˜𝜽  ,

where 𝐶 ℎ (𝜅 ) is the normalizing constant 

𝐶 ℎ (𝜅 ) = 𝜅 ℎ/2−1

(2𝜋 )ℎ/2 𝐼 ℎ/2−1 (𝜅 ) ,

and 𝐼 𝜈 (·) denotes the modified Bessel function of the first kind of order 𝜈 .The parameter 𝜽 represents the mean direction of the distribution, while 𝜅 controls the concen-tration of the distribution around 𝜽 . When 𝜅 = 0, the distribution reduces to the uniform distribution over Sℎ−1. As 𝜅 → ∞ , the distribution becomes increasingly concentrated around 𝜽 .In our experiments, the vMF distribution is used to model perturbations of the parameter vector in the objective-space error setting, as it naturally respects the unit-norm constraint on 𝜽 .

EC.2. Posterior Visualizations for Examples 1 and 2 

Figure EC.1 shows histograms of 10,000 MCMC posterior samples (post warm-up) of the angular coordinate of 𝜽 drawn from Algorithms 1 and 2 applied to the data in Examples 1 and 2, respectively. In each case, we use a von Mises–Fisher distribution ( 𝜅 = 5) for the proposal distribution 𝑞 (· | 𝜽 ).In Example 1 (decision-space error) with linear constraints, the step-like structure of the posterior arises from the many-to-one mapping between 𝜽 and x★(u𝑖 , 𝜽 ). This means the likelihood of 

x★(u𝑖 , 𝜽 ) depends on 𝜽 only through the “arc” in which it lies. Since all 𝜽 in the same arc share an identical likelihood and the prior is uniform, the posterior is constant within each arc and jumps only at the arc boundaries. In contrast, in Example 1 with quadratic constraints, there is a one-to-one mapping between 𝜽 and x★(u𝑖 , 𝜽 ), so the likelihood varies smoothly with 𝜽 and the posterior exhibits a continuous shape. Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

25 π 4             

> π23π4
> π
> 5π43π23π40 /
> θ^θ*
> Linear Constraints π4
> π23π4
> π
> 5π43π23π40 /
> θ^θ*
> Quadratic Constraints

(a) Example 1 π 4              

> π23π4
> π
> 5π43π23π40 /
> θ^θ*
> Linear Constraints π4
> π23π4
> π
> 5π43π23π40 /
> θ^θ*
> Quadratic Constraints

(b) Example 2 

Figure EC.1 Histograms of 10,000 posterior samples (post warm-up) of the angular coordinate of 𝜽 . Panel (a) corresponds to Example 1 (decision-space error) and panel (b) to Example 2 (objective-space error). In each plot, the dashed black line indicates the direction of the true parameter 𝜽 ∗ and the solid blue line indicates the posterior mean direction ˆ𝜽 .

Interestingly, Example 2 (objective-space error) with linear constraints does not yield a step-like posterior. This is because the likelihood 𝜈 (y𝑖 | x★(u𝑖 , 𝜽 )) = ∫

{ ˜𝜽 ∈Θ | x★ (u, ˜𝜽 )=y} 𝑝 ( ˜𝜽 | 𝜽 ) 𝑑 ˜𝜽 (Equation (10) in Section 5.4) depends on 𝜽 through the integral of a smooth perturbation density (vMF) over the fixed arc  ˜𝜽 ∈ Θ x★(u, ˜𝜽 ) = y . Thus, when there is a many-to-one mapping from 𝜽 to x★(u𝑖 , 𝜽 ),the likelihood varies smoothly with 𝜽 , producing a continuous posterior. With quadratic constraints, the one-to-one mapping between 𝜽 and x★(u𝑖 , 𝜽 ) implies that the likelihood is determined by the unique perturbed cost recovered from y𝑖 via (9), i.e., 𝜈 (y𝑖 | x★(u𝑖 , 𝜽 )) = 𝑝 ( ˜𝜽 𝑖 | 𝜽 ), where ˜ 𝜽 𝑖 is the unique perturbed cost associated with y𝑖 , and is thus shaped by the smooth vMF likelihood. 

EC.3. Numerical Experiment Details 

EC.3.1. Models 

In our experiments, we assume ℎ = 𝑛 . Let x ∈ R𝑛 be the decision vector. Let A ∈ R𝑚 ×𝑛 , b ∈ R𝑚 ,

Q ∈ R𝑛 ×𝑛 be a positive definite matrix, and e be the vector of all ones. The two models we consider are 

LP : minimize 

x 𝜽 ⊤x

subject to Ax ≥ b,

− e ≤ x ≤ e

and 

QP : minimize 

x 𝜽 ⊤x

subject to x⊤Qx + e⊤x ≤ 1.Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 26

The box constraints are included to ensure the LP has a finite optimum. For the LP, we set (𝑚, 𝑛 ) = (75 , 20 ). The rows of A ∈ R𝑚 ×𝑛 are generated by sampling ˜ a𝑗 ∼N ( 0, I𝑛 ), normalizing, and scaling by independent factors 𝑠 𝑗 ∼ Unif (0.7, 1.4), i.e., 

a𝑗 := 𝑠 𝑗 

˜a𝑗 

∥ ˜a𝑗 ∥2

, 𝑗 = 1, . . . , 𝑚. 

We then sample w ∼ Unif ([− 0.72 , 0.72 ]𝑛 ) and define b = Aw − 𝜹 , where 𝜹 ∼ Unif ([ 0.04 , 0.28 ]𝑚 ).This ensures strict feasibility of w for the constraints Ax ≥ b. The parameter ranges are chosen to yield well-conditioned instances; we verified that the results are robust to moderate variations in these ranges. For the QP, we set 𝑛 = 20 and generate matrices Q with eigenvalues uniformly distributed between 1 and 5. 

EC.3.2. Datasets 

For each formulation, a dataset D = {( u𝑖 , y𝑖 )} 𝑁 𝑖 =1 was generated comprising 𝑁 instances. For instance 𝑖 , the covariate u𝑖 consists of (A𝑖 , b𝑖 ) for the LP and Q𝑖 for the QP. We set the true parameter vector to 𝜽 ∗ = e/√𝑛 . To generate y𝑖 in the decision error model, we solve 𝑁 instances of the forward problem to obtain optimal solutions x★(u𝑖 , 𝜽 ∗), 𝑖 = 1, . . . , 𝑁 , and generate observations as 

y𝑖 = x★(u𝑖 , 𝜽 ∗) + 𝝐 𝑖 ,

where 𝝐 𝑖 ∼ N ( 0, 𝜎 2I) are independent across instances. For the objective error model, we draw perturbed parameter vectors independently from a von Mises–Fisher distribution on S𝑛 −1 with mean direction 𝜽 ∗ and concentration parameter 𝜅 , and solve the forward problems to optimality to obtain y𝑖 , 𝑖 = 1, . . . , 𝑁 .

EC.3.3. Solving the Inverse Optimization Model in Algorithm 2 

For the LP, we determine active constraints at the observed (u𝑖 , y𝑖 ) and choose ˜ 𝜽 𝑖 as a normalized vector in the corresponding normal cone. For the QP, we set ˜ 𝜽 𝑖 = (− 2Q𝑖 y𝑖 − e)/(∥ − 2Q𝑖 y𝑖 − e∥2),which is the normalized vector orthogonal to the feasible region defined by (u𝑖 , y𝑖 ).

EC.3.4. Proposal Distribution 

To generate 𝜽 𝑐 , we sample z ∼ N ( 𝜽 𝑡 −1, S) and project to the hypersphere via 𝜽 𝑐 ← z/∥ z∥2. Because the Gaussian distribution has support on R𝑛 rather than S𝑛 −1, this approach induces a proposal Chan, Sandholtz, and Yousefi: Uncertainty Quantification in IO 

> 27

density that is not exactly symmetric, so the acceptance probability should include the proposal ratio 

𝑞 (𝜽 𝑡 −1 | 𝜽 𝑐 )/ 𝑞 (𝜽 𝑐 | 𝜽 𝑡 −1). In our implementation, however, we adopt a symmetric approximation for numerical efficiency. This is justified because when the largest eigenvalue of S is small (i.e., 

≪ 1), proposals remain in a small neighborhood of 𝜽 𝑡 −1 on S𝑛 −1 and the sphere is approximately flat locally, so the proposal ratio is close to 1 and the resulting bias is negligible. Under posterior consistency (established in Theorem 1), the posterior concentrates around 𝜽 ∗ as 𝑁 → ∞ , implying that well-calibrated proposals will have small step sizes. The near-nominal empirical coverage observed in our numerical experiments supports the validity of this approximation. To improve sampling efficiency, we adopt the adaptive Metropolis scheme of Haario et al. (2001), which updates the proposal covariance matrix S during the warm-up phase to target an acceptance rate between 25% and 40%. We experimented with two alternative symmetric proposal distributions: (i) a Gaussian distri-bution with mean 𝜽 𝑡 −1 and isotropic covariance 𝜎 2 

> 𝑝

I, with 𝜎 2 

> 𝑝

adapted during warm-up, and (ii) a vMF distribution with mean direction 𝜽 𝑡 −1 and concentration parameter 𝜅 𝑝 , also adapted during warm-up. We compared these to the Gaussian proposal with the full covariance matrix S across LP and QP instances of dimension 𝑛 ranging from 2 to 20. In low dimensions ( 𝑛 ≤ 5), all three proposals yielded comparable posterior summaries ( 𝛼 RMS and coverage) and similar sampling effi-ciency. In higher dimensions, the full-covariance Gaussian proposal substantially outperformed both alternatives in efficiency, requiring substantially fewer iterations (by an order of magnitude), to reach comparable PSRF values. This reflects its ability to adapt to the shape of the posterior through 𝑛 (𝑛 + 1)/ 2 covariance parameters, rather than the single scalar of the isotropic Gaussian or vMF proposals. 

EC.4. Code and Data 

Code and data can be found at https://github.com/nyousefi2020/UQ_in_IO . All experiments were run on a MacBook Pro (2021) with an Apple M1 Max processor and 64 GB of memory. Optimization problems were solved using Gurobi (Gurobi Optimization, LLC 2026).